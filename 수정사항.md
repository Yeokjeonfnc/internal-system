# 수정사항 로그


## 2026-07-09 — 전체 점검

### 1. 빌드 환경 (치명 결함 수리)
| pub 캐시 `webview_windows-0.4.0` | 손상(빈 폴더) 삭제 후 재다운로드 | analyze 에러 8건의 원인 — Windows 데스크톱 빌드 불가 상태였음 |
| pub 캐시 `jni`, `path_provider_windows/linux`, `shared_preferences_windows/linux`, `url_launcher_windows/linux` (6종) | 손상 항목 전수 스캔 후 삭제·재다운로드 | 매 빌드마다 뜨던 "plugin does not exist" 경고의 원인. **Windows 앱의 파일저장·링크열기·설정저장 기능이 통째로 빠진 채 빌드되던 상태** |

### 2. Flutter 정적분석 클린업 (34건 → 0건)
| `bbs001_view.dart` (게시판) | 게시글 로드 실패·삭제 실패 시 화면이 이미 닫힌 뒤 다이얼로그를 띄우려던 2곳에 mounted 가드 추가 | 화면 이탈 직후 응답 도착 시 크래시 가능 (실버그) |
| `str001_store_nfc_tag_panel.dart` | NFC 등록 후 재조회 다음에 mounted 재확인 추가 | 위와 동일한 async-context 실버그 |
| `mst001_view.dart` / `mst001_view_detail.dart` | 항상 기본값만 쓰는 `_YnFieldChip` 생성자 파라미터 제거, 상세 쪽 칩 색을 디자인 토큰으로 통일 | 미사용 파라미터 정리 + 팔레트 일관성 |
| `code_option.dart` / `str001_model.dart` (HistoryEntry) | 커스텀 fromJson을 쓰므로 `createFactory: false` / 어노테이션 제거 후 build_runner 재생성 | 생성만 되고 안 쓰는 코드 제거 |
| `usage_log_api` `dev003_api` `bbs001_api` `store_entry_api` | `if (x != null) 'k': x` → `'k': ?x` (null-aware 요소) 12곳 | Dart 3 권장 문법, 코드 간결화 |
| `dev002_filter` `store_entry_map_io` `act002_note_tab_view` `dev002_view_detail` `dsh001_screen` | 미사용 import·메서드·변수 5건 삭제 | 데드코드 제거 |
| `pubspec.yaml` | `http_parser` 직접 의존성 선언 | 간접 의존성을 직접 import 중이었음(버전 변동 위험) |
| `api_base_url_config.dart` `act004_model.dart` | 문서주석 형식·불필요 인터폴레이션 정리 | lint 정리 |

### 3. 성능
| `dsh001_screen.dart` (대시보드) | `FutureBuilder`의 future를 build()에서 생성하던 것을 State에 1회 캐시 | **리빌드될 때마다 API 4개(알림·가맹점·활동 등)를 재호출**하던 버그 — 대시보드 버벅임의 주원인 |
| (점검) 전 화면 future-in-build 패턴 전수 검색 | 대시보드 외 추가 발생처 없음 확인 | 동일 유형 버그 재발 방지 확인 |
| (점검) 메신저 채팅 스크롤 | `addPostFrameCallback` 사용 확인 — 수정 불필요 | 빌드 중 스크롤 조작 안전성 확인 |

### 4. 데드코드·미사용 파일 삭제
| `pages/dashboard/dsh001/` 8개 파일 (kpi_card, kpi_model+.g, kpi_json_keys, summary_slot, view_model, franchise_contract_card+data) | 파일 삭제 + `app_data_refresh.dart`의 참조 제거 | 구 대시보드 잔재 — provider가 어디서도 watch되지 않는 완전 데드체인 |
| `core/network/dio_provider.dart` | 삭제 (폴더째) | 미사용 + `localhost:8000` 오설정까지 있던 죽은 코드 |
| `core/widgets/common/common_detail_button.dart` | 삭제 | 어떤 화면도 사용하지 않음 |
| `pages/active/act003/act003_api.dart`, `act003_model.dart` | 삭제 | act002 별칭/재수출 셸 — 참조 0 |
| `core/theme/shell_tab_chrome.dart` | 다크 그라데이션·그림자 등 미사용 상수 제거, 사용 중인 6개만 유지 | 라이트 전환 후 잔재 정리 |

### 5. 스크립트·플랫폼
| `run_flutter_web.bat` | `PROJECT_DIR` 하드코딩(`C:\y-on\app_flutter`) → 스크립트 기준 상대경로(`%~dp0`), 폴백 Flutter를 프로젝트 고정 버전(v3_41_4)으로 | 이 저장소 위치에서 실행하면 즉시 실패하던 경로 + quill 비호환 stable 폴백 문제 |
| PC웹/모바일웹 | analyze 0건 + 웹 릴리즈 빌드 성공, 컴팩트(<840px) 분기 21개 파일 커버 확인 | 반응형 대응 확인 |
| Windows 앱 | pub 캐시 수리로 path_provider/url_launcher/shared_preferences Windows 구현체 복원 | 데스크톱 앱 파일·링크·설정 기능 정상화 |
| Android 앱 | 이 PC에 Android SDK 없음 → 빌드는 개발머신에서 필요 (코드·매니페스트는 analyze 통과) | 환경 제약 기록 |

### 6-A. 로딩 체감속도 최적화 (클릭 → 즉시 표시)
> 측정: 모든 API에 **~0.7초 왕복 지연 바닥**(터널 RTT), 가맹점 목록 1.3초/144KB. → 재방문 캐시·병렬화·배경갱신으로 해결.

| `core/perf/session_list_cache.dart` (신규) | 세션 메모리 목록 캐시(stale-while-revalidate 공용 유틸) | 재방문 화면이 매번 스피너로 비는 것이 체감 저하의 주원인 |
| 대시보드 `dsh001_screen.dart` | ① 직렬 3회 API 호출 → **병렬**(2.8초→1.3초) ② keepAlive Provider로 세션 캐시(재진입 0초) | 홈 진입이 가장 잦은 동선 |
| 활동관리/활동결재 목록 `act002_widget_drafts.dart`, `act002_note_tab_view.dart` | FutureBuilder → 세션캐시+마지막 데이터 유지, 갱신 중엔 상단 2px 진행바만 표시 | 탭 전환·재진입마다 스피너+0.8초 대기하던 화면 → 즉시 표시 |
| 게시판 `bbs001_view.dart` | ① 폴더·게시글 **직렬 2회 → 병렬 1회** ② 폴더·목록 세션캐시로 재진입 즉시 표시 | 게시판 진입 1.6초 → 0.8초(첫 진입) / 0초(재진입) |
| 부서관리 `mst002_view.dart` | 트리 세션캐시 + 배경 갱신 | 재진입 즉시 표시 |
| 가맹점 목록 `str001_controller/view` | 진입 시 provider 6개 전체 invalidate → **목록만** 배경 갱신 (새로고침 버튼은 기존대로 전체) | 진입마다 불필요한 왕복 6회 제거 — 코드/지역/브랜드는 세션 캐시 유지 |
| `app_data_refresh.dart` | 저장·삭제 후 전역 갱신 시 세션캐시 clearAll + 대시보드 provider invalidate 연결 | 캐시 도입에 따른 신선도 보장 |
| `backend/application.yml` | HTTP 응답 gzip 압축 활성화(`server.compression`) | 가맹점 목록 144KB 등 대용량 JSON 전송량 절감 — **다음 백엔드 배포 시 적용** |

**발견(별도 조치 필요)**: 라이브 서버(`test.yeokjeon.com`)의 백엔드 JAR가 구버전이라 `GET /api/posts`가 500 반환(게시판·메신저 백엔드 미포함 시점 빌드). 서버 PC에서 새 JAR 배포+재시작 필요.

### 6-B. 필드 서버 운영성 고도화 (`deploy/package/field-server` 템플릿 + `C:\y-on\yon-field-server` 라이브 동기화)
| 어디를 | 어떻게 | 왜 |
|---|---|---|
| `scripts/backup-db.ps1` (신규) | pg_dump 커스텀 포맷으로 `backups\`에 저장, 기본 14개 보존, `logs\backup.log` 이력 | **백업이 전무하던 상태** — 운영 DB 최대 리스크 해소 |
| `scripts/install-backup-task.ps1` / `uninstall-backup-task.ps1` (신규) | 매일 03:00 자동 백업 스케줄 등록/해제 | 수동 백업 의존 제거 |
| `scripts/update-app.ps1` (신규) | 릴리즈 zip/폴더 하나로 정지→현재버전 백업(`releases\backup-<ts>`)→교체→기동→헬스체크→`logs\deploy-history.log` 감사 기록(작업자·시각·JAR SHA256) | 배포가 수동 복사였고 이력이 없었음 — **구버전 JAR 방치(posts 500) 같은 사고 재발 방지 + 원커맨드 롤백 경로** |
| `scripts/status.ps1` (신규) | 프로세스·포트·HTTP 응답시간·DB 접속·디스크·배포 버전(JAR/웹 빌드 시각)·최근 백업·최근 오류를 한 화면에 | 장애 시 원인 파악을 1개 명령으로 |
| `scripts/db-maintenance.ps1` (신규) | VACUUM ANALYZE + 테이블 크기 상위 10개 리포트, 이력 로그 | 장기 운영 시 팽창 방지 |
| `scripts/start-backend.ps1` | 기동 시 backend.out/err 로그를 타임스탬프로 로테이션(최근 10개 유지) | 무한 증식하던 단일 로그 → 재기동 간 구간 보존(장애 분석 가능) |
| `caddy/Caddyfile.http` + `start-web.ps1` | 접근 감사 로그(JSON, 20MiB 롤링·10개·30일) `logs\caddy\access.log` 추가 | 누가 언제 어떤 API/경로를 호출했는지 감사 가능 — **Caddy 재시작 후 활성** |
| `README.md` | "운영·유지보수 명령" 섹션 신설(배포/롤백/백업/복원/상태/로그 위치) | 담당자 인수인계·운영 문서화 |
| (점검) `usage_log` 테이블 | 인덱스(used_at/user_id/use_type) 이미 적정 — DDL 불필요 확인 | DB 감사 기반 확인 |

> 반영 방식: 저장소 템플릿과 라이브 폴더(`C:\y-on\yon-field-server`) 모두 갱신(Resilio 로 서버 PC 자동 전파). 실행 중인 프로세스는 건드리지 않았으며, 접근로그는 다음 웹 재시작, gzip 압축은 다음 JAR 배포부터 적용.

### 6. 백엔드 점검 결과 (추후 봐꿔야될거)
| `application.yml` | (수정 안 함) DB 비밀번호·Kakao 키가 로컬 기본값으로 하드코딩. 필드 서버는 기동 인자로 덮어써서 동작엔 문제 없으나, 사설 저장소 밖 공유 금지

### 7. 2026-07-14 — 로컬 웹 빌드 재점검
| `app_flutter/.dart_tool/package_config.json` | 삭제 후 `flutter pub get` 재실행으로 재생성 | 예전 사용자 계정("minhyo") 시절 pub 캐시 절대경로가 그대로 남아있어 `flutter build web` 컴파일 자체가 실패하던 상태 — 이전 세션의 Puro 환경 수정으로도 이 캐시 파일은 갱신되지 않았음. 지금까지의 로컬 빌드 확인이 실제로는 이 문제 때문에 최신 소스 반영이 안 됐을 가능성 있음 |
| (재빌드) `flutter build web --dart-define-from-file=dart_defines.local.json` | 위 수정 후 재실행 — 경고 없이 정상 완료(wasm dry-run 정보성 경고만 존재) | 162개 수정 파일(테마·STEP1-9·ACT/MST·턴7·성능 최적화 전체) 반영 확인 |
| 로컬 정적 서버 | `build/web`을 detached 프로세스로 포트 3050 서빙, API는 `test.yeokjeon.com` 실서버 연동 | 사용자 로컬 테스트용 |
| pub 캐시 `file_picker-10.3.10` | 손상(빈 폴더) 재발견 → 삭제 후 `pub get` 재수신 | §1의 minhyo 경로 수정과는 별개로, 이번엔 현재 계정 캐시 자체가 손상되어 있어 `flutter build web`이 또 실패 — 전체 pub_cache 무결성 스캔(0건 추가 발견) 후 정상 빌드 확인

### 8. 2026-07-14 — 대시보드 반응형 그리드 + 랜딩 라이트/다크
| `core/widgets/common/common_responsive_grid.dart` (신규코드 생성햇음) | 카드 최소 너비 기준으로 열 수를 계산해 `Wrap`으로 배치하는 `ResponsiveCardGrid` 위젯 | 대시보드가 `width > 1100` 단일 기준점만 있어 그 아래로 내려가면 KPI 카드 4개 + 콘텐츠 카드 4개가 전부 세로 한 줄로 쌓이던 문제 — 너비에 맞춰 매끄럽게 열 수가 줄어들도록 교체

### 9. 2026-07-14 — 신규 로고(logo1.png) 교체
| `assets/images/logo_yj.png`, 웹 favicon·PWA 아이콘 5종, Android mipmap 5종, iOS AppIcon.appiconset 15종, Windows `app_icon.ico` | 사용자 제공 `logo1.png`(빨간 Y 모노그램)를 정사각 패딩 후 각 플랫폼 규격 크기로 재생성해 일괄 교체 | 기존엔 `flutter_launcher_icons` 같은 자동화 도구가 아예 없어 전 플랫폼이 Flutter 기본 템플릿 아이콘(파란색)이었음
| `core/auth/login_view.dart` | 로그인 화면 상단의 `Container`+`Text('역')` 코드로 그린 배지를 `Image.asset('assets/images/logo_yj.png')`로 교체 | 사이드바(`main_frame_layout.dart`의 `_SidebarBrand`)는 이미 이미지 참조 방식이라 파일 교체만으로 충분했지만, 로그인 화면은 텍스트로 직접 그리고 있어 코드 교체가 필요했음 |
| `web/landing.html` | 내비게이션 바의 텍스트 배지를 `<img src="icons/Icon-192.png">`로 교체

### 10. 2026-07-14 — 실서버 로그인 불가("localhost:3001") 원인·수정
| `app_flutter/dart_defines.local.json`의 `API_BASE_URL` | `https://test.yeokjeon.com/api` → `http://localhost:3001/api`로 값이 바뀌어 있던 것을 원복 | 이 파일은 `.gitignore` 대상(머신별 로컬 설정)인데 레실리오 동기화 폴더 안에 있어, 로컬 개발용 값을 쓰는 다른 PC/세션과 계속 동기화되며 덮어써진 것으로 추정 — 이 값이 빈 채(또는 로컬값)로 빌드되면 웹은 `localhost:3001`로 폴백해 "서버에 연결할 수 없습니다" 오류가 뜸 |
| `C:\y-on_share\.sync\IgnoreList` | `dart_defines.local.json`을 동기화 제외 목록에 추가 | 재발 방지 — 레실리오 앱 재시작 후 적용 |
| `pages/eap/eap001/eap001_detail_view.dart` | 재빌드 중 별개로 발견된 컴파일 에러 수정 — `DetailScreenScrollBody(title: ...)`에 필수 인자가 빠져 있던 것을, 기존 헤더 Row를 `title:`로 옮기고 중복된 바깥쪽 스크롤 래퍼 제거 | 작업 중이던 다른 변경분과 겹쳐 발생한 것으로 보임 — 위젯 자체의 "배경+제목+스크롤 본문" 설계에 맞춰 기계적으로 정리 |
| 재빌드 후 `C:\y-on\yon-field-server\web\`에 반영 | `main.dart.js`에 `test.yeokjeon.com` 포함/`localhost:3001` 미포함 확인 후 배포(재시작 불요) | 실사용자 로그인 불가 상태 해소 |

### 11. 2026-07-14 — 로컬 실행용 `run_local_test.bat` 신설 + 관련 버그 3종
| `run_local_test.bat` (신규, 저장소 루트) | `--dart-define`을 커맨드라인에 직접 넘겨 실행 — `dart_defines.local.json` 파일 자체를 안 씀 | §10의 재발 방지(파일이 아니므로 동기화 충돌 여지가 아예 없음) + 다른 개발자도 그대로 공유해서 각자 실행 가능 |
| (확인) `flutter run`(핫리로드 개발 서버) | 이 PC의 Puro 설치본에서 "SDK root directory not found: /C:/..." 로 항상 실패하는 것 재확인(파일은 실제 존재함 — 경로 인식 자체의 툴 버그) | `run_flutter_web.bat`도 동일 증상 — 정상 동작이 아니라 알려진 결함. `flutter build web` + 정적 서버 방식으로 우회 |
| `run_local_test.bat` 내 Puro 환경 선택 순서 | `puro.bat`(활성 환경, 이 PC는 `stable`=3.44.0)을 우선 쓰던 로직 → 프로젝트 고정 버전(`v3_41_4`) 경로를 우선하도록 수정 | 잘못된 버전으로 빌드되면 `flutter_quill`이 최신 stable Flutter API와 안 맞아 컴파일 실패(`TextInputClient.onFocusReceived` 누락) — `run_flutter_web.bat`에도 같은 결함이 있었음 |
| `run_local_test.bat` 내 `flutter build web` 호출 | `call`로 직접 부르던 것 → `cmd /c "..."`로 격리해서 실행 | `flutter.bat` 내부가 `exit`(`exit /b`가 아님)로 끝나는 경로가 있어, `call`로 부르면 빌드 성공 직후 상위 스크립트 전체가 같이 죽어 그 뒤(포트 확인·서빙 시작)가 전혀 실행되지 않던 문제 |
| `run_local_test.bat` 인코딩 | LF 줄바꿈 + 캐럿(`^`) 줄연속 조합이 깨져서 `--dart-define=...` 인자가 토큰별로 쪼개지던 것 → 명령을 한 줄로 합치고 CRLF로 정규화 | Write 도구가 LF로 저장 → cmd.exe 배치 파일은 CRLF 기준이라 캐럿 연속이 불안정함 |
| (참고) 레실리오 `.lock` 실패 증상 | `.git` 폴더가 동기화 대상에 포함돼 있어, 양쪽 PC가 동시에 git 명령을 쓰면 `.git/index.lock`가 순간적으로 생겼다 사라지며 동기화 실패로 보일 수 있음(재현되는 상시 lock 파일은 현재 없음) | 근본 해결은 `.git`도 IgnoreList에 추가하는 것 — 아직 미적용, 다음 작업 때 반영 권장 |

### 12. 2026-07-14 — 필드서버 Caddy 기동 실패("caddyccess.log") 원인·수정
| `caddy/Caddyfile.http`(템플릿 + 라이브) | 접근 로그 경로의 `caddy\access.log`에서 `\a`가 실제 벨 제어문자(0x07)로 저장되어 `caddy` + (보이지 않는 문자) + `ccess.log`가 되어 있던 것을 정상 경로로 복구 | 예전 세션에서 이 로그 블록을 추가할 때 생긴 잠복 결함 — 그 이후 Caddy를 재시작한 적이 없어 지금까지 안 걸리다가, 오늘 랜딩페이지 라우팅 반영을 위해 처음 재시작하면서 `Error: loading initial config: ... open ...\logs\caddyccess.log: 파일명... 구문이 틀렸습니다`로 Caddy 기동 자체가 실패 — 웹 전체가 죽어있던 상태였음 |

### 13. 2026-07-14 — 랜딩페이지 로그인 버튼이 실제로 로그인까지 안 이어지던 문제
| `web/landing.html` 로그인 링크 3곳 | `href="/#/login"` → `href="/index.html#/login"`로 변경 | 랜딩페이지 자체가 `/`에서 서빙되는 상태라 `#/login`은 "같은 문서 내 프래그먼트 이동"으로 처리돼 브라우저가 새로고침을 안 함 — 그 결과 URL만 `#/login`으로 바뀌고 실제 Flutter 앱(`main.dart.js`)은 전혀 로드되지 않던 상태였음. 다른 문서(`index.html`)를 명시해 강제로 새 페이지 로드가 일어나게 수정. 실제 브라우저 클릭 테스트로 `main.dart.js` 로드 확인 |

### 14. 2026-07-14 — 사원관리(mst001) 목록 정리 + CSV 일괄 다운/업로드
| `pages/master/mst001/mst001_view.dart` 테이블 헤더 | 입사년월일 컬럼 삭제, "아이디"→"로그인ID" | 요청에 맞춰 목록 컬럼 정리 |
| `pages/master/mst001/mst001_csv.dart` (신규) | CSV 인코딩/디코딩 유틸(엑셀 한글 깨짐 방지 BOM, 따옴표 이스케이프 처리) | 일괄 다운로드/업로드 기반 |
| `core/widgets/common/common_list_page_template.dart` | `extraHeaderActions` 파라미터 신설(등록 버튼 왼쪽에 보조 버튼 삽입 가능) | mst001 전용 훅이 아니라 다른 목록 화면에서도 재사용 가능하게 공용 템플릿에 추가 |
| `mst001_view.dart` 일괄 다운로드/업로드 버튼 | `FilePicker.platform.saveFile`/`pickFiles`로 CSV 저장·업로드. 업로드는 로그인ID로 기존 사원을 찾아 이름·부서·직급·휴대전화·이메일·태그사용여부만 수정(신규 계정 생성은 CSV로 안 함 — 비밀번호가 평문으로 파일에 남는 걸 피하기 위해 기존 등록 화면으로 유지) | |
| (버그 수정) CSV 업로드 후 상세화면에 반영 안 되던 문제 | 업로드 성공한 사원마다 `ref.invalidate(userDetailProvider(userIdx))` 추가(기존 목록 provider만 무효화하고 있었음) | 단건 수정 화면(`mst001_view_detail.dart`)은 원래 목록+상세 캐시를 같이 무효화하는데, CSV 업로드 경로만 그 패턴을 안 따라가고 있었던 것이 원인 |
| (기능 추가) 업로드 전 확인 절차 | `_UploadPreviewDialog` — 실제로 반영되기 전, 어떤 사원의 어떤 필드가 어떻게 바뀌는지(변경 전→후) 미리 보여주고 승인해야 저장이 진행되도록 변경 | 재확인 없이 바로 저장되던 것을 요청에 맞춰 수정 |
| (수정) 업로드 미리보기를 diff 방식에서 결과표 방식으로 변경 | 기존 값과 "무엇이 바뀌는지" 비교하던 로직을 제거하고, CSV 내용이 그대로 어떤 표가 되는지(이름·부서·직급·휴대전화·로그인ID·이메일·태그사용여부 전체)를 보여주는 방식으로 교체. 로그인ID로 매칭 안 되는 행은 표에서 강조 표시하고 저장 대상에서 제외 | "특정 아이디 기준으로 비교하지 말고, 파일 전체가 완전히 달라져도 그 데이터 그대로 저장되는 걸 확인하는 방식이면 된다"는 피드백 반영 — 매칭은 저장 시 어느 사원 레코드인지 식별하는 데만 쓰고, 화면에는 비교 없이 결과만 보여줌 |

### 15. 2026-07-15 — 사이드바 메뉴 라벨·순서·전자결재 구조 재정리
| `core/layout/main_frame_layout.dart` — 활동 관리 카테고리 | 카테고리명 "활동관리"→"활동 관리". 하위 항목 "활동관리"→"활동 등록"(활동현황 위로 순서 이동), "활동현황"→"활동 현황" | 요청 반영 |
| `core/layout/main_frame_layout.dart` — 전자결재 | 클릭 시 바로 이동하는 단일 메뉴 항목 → 개발 관리·마스터 관리와 동일한 펼침 카테고리로 변경. 하위 13개 항목(전자결재 홈·결재 대기/수신 문서·참조열람 대기·결재 예정·기안/임시저장/결재/참조열람/수신/발송/공문 문서함·환경설정)을 사이드바 하위 메뉴로 노출 | "전자결재 클릭 시 오른쪽에 별도 탭(자체 좌측 네비 패널)이 다시 생기는" 것을 다른 목록처럼 사이드바 트리로 재정리해달라는 요청 반영 |
| `pages/eap/eap001/eap001_shell.dart` | 내부 좌측 네비 패널(`EapNavPanel`)·모바일 칩 네비(`_CompactEapNav`) 제거 — 본문만 렌더링 | 메인 사이드바가 이동을 전담하게 되어 이중 네비게이션(사이드바 + 전자결재 자체 좌측 패널)이 되던 것을 제거 |
| `pages/eap/eap001/eap001_widgets.dart` | 이제 아무 데서도 안 쓰는 `EapNavPanel`/`_EapNavSection`/`_EapNavItem` 및 그 전용 import(`go_router`, `mock_eap_documents`, `eap_routes`) 삭제 | 데드코드 정리 |

### 16. 2026-07-15 — 전자결재 사이드바 그룹 소제목 + 헤더 클릭 시 홈 이동
| `core/layout/main_frame_layout.dart` — `_SidebarGroupLabel` (신규) | 클릭 불가 소제목 위젯 신설 | 예전 전자결재 자체 좌측 패널에 있던 "결재하기"/"개인 문서함" 그룹 구분을 사이드바 하위 목록에도 복원 |
| `core/layout/main_frame_layout.dart` — 전자결재 하위 목록 | "전자결재 홈" 항목 삭제, "결재하기"(결재대기/수신/참조열람대기/결재예정) · "개인 문서함"(기안/임시저장/결재/참조열람/수신/발송/공문 문서함) 두 그룹으로 소제목 삽입, 환경설정은 그룹 밖 하단에 유지 | 요청 반영 |
| `core/layout/main_frame_layout.dart` — `_SidebarExpandableMenuItem` | `onHeaderTap` 파라미터 신설 — 지정 시 상위 항목 클릭이 접기/펴기 토글 대신 해당 이동 실행 + 목록을 펼침(닫지 않음). 미지정 시(개발관리·마스터관리·활동관리) 기존 토글 동작 그대로 | 전자결재 카테고리 헤더 클릭 시 "전자결재 홈"을 누른 것과 동일하게 이동하면서 목록도 펼쳐지도록 요청 반영 — 다른 카테고리는 홈 개념이 없어 동작 변경 없음 |

## 2026-07-31 — 보안 점검

### 17. 점검 결과 요약
전체 백엔드·통신 경로 점검. **양호 확인**: SQL 인젝션 없음(MyBatis 매퍼 22개 전수 확인 — `${}` 4곳은 하드코딩 화이트리스트 + `information_schema` 검증 경유라 주입 불가), 업로드 경로 탈출(path traversal) 차단됨(6개 서비스 모두 UUID 파일명 + `normalize()` + `startsWith` 가드), 다운로드 경로 탈출 불가(파일명이 DB 출처), Actuator 등 디버그 엔드포인트 없음, CORS 와일드카드 없음.

**외부 노출 범위 실측**: `test.yeokjeon.com` 기준 5432/5433(DB)·3001(백엔드) 모두 차단됨, 8080(Caddy)만 개방 — DB 직접 공격 경로는 없음.

### 18. 조치 완료 (이번 세션)
| `backend/application.yml` | DB 비밀번호(평문)·Kakao REST API 키를 환경변수 참조(`${DB_PASSWORD:}`, `${KAKAO_REST_API_KEY:}`)로 교체 | **저장소가 GitHub 공개(public) 상태**여서 두 값이 인터넷에 그대로 노출돼 있었음. 운영 서버는 원래 `start-backend.ps1`이 `backend.env` 값을 주입하므로 동작 영향 없음. **단, 이미 유출된 값이므로 교체(rotate) 필요 — 아래 §19** |
| `backend/application.yml` 로깅 | mapper `DEBUG` + `BasicBinder TRACE` → 환경변수 기본 `INFO`/`WARN` | SQL 바인딩 값이 전부 로그 파일에 남는 설정이었음. 로그인 쿼리의 **평문 비밀번호와 개인정보가 디스크에 기록**되던 상태(운영 프로필에도 동일 적용됨) |
| `chat/controller/ChatController.java` | 첨부 다운로드 시 inline 허용을 안전한 래스터 이미지(jpeg/png/gif/webp/bmp)로 제한, 그 외는 `attachment` + `application/octet-stream` 강제, `X-Content-Type-Options: nosniff` 추가 | 업로더가 보낸 Content-Type을 그대로 믿고 `image/*`면 inline 으로 내려주고 있었음 → SVG에 스크립트를 넣어 올리면 같은 출처에서 실행되는 **저장형 XSS**(대화방 참여자 대상) |
| `chat/ws/ChatWebSocketConfig.java` | WebSocket 허용 출처 `"*"` → CORS 설정과 동일한 5개 출처로 제한 | 인터넷의 **아무 웹사이트나** 이 소켓에 접속 가능한 상태였음(네이티브 앱은 Origin 헤더가 없어 영향 없음) |
| `caddy/Caddyfile.http`(라이브+템플릿) | `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` 추가 및 `Server` 헤더 제거 | 클릭재킹·MIME 스니핑·리퍼러 유출 기본 방어. **Caddy 재시작 후 적용** |

### 19. 미조치 — 사용자 결정/작업 필요 (심각도순)
| 1. **API 인증 부재 (치명)** | Spring Security가 `pom.xml`에서 주석 처리돼 있고 필터·인터셉터·세션·토큰이 **전무**. 전체 ~123개 엔드포인트가 인증 없이 호출 가능. 로그인은 사용자 정보만 반환할 뿐 이후 요청은 `?userId=` 쿼리 파라미터로 신분을 자칭. `?userId=admin` 이면 `application.yml`의 `super-admin-user-ids: admin` 때문에 슈퍼관리자로 통과 | 인터넷에서 누구나: 계정 생성·삭제, 타인 프로필 수정, **본인에게 전 메뉴 권한 부여**(`PUT /users/{id}/menu-permissions`), 전 직원 DM 열람, 매장·물건 문서 전부 다운로드 가능. 근본 수정은 토큰 인증 도입 + 클라이언트 전면 수정이 필요한 대공사 |
| 2. **유출 자격증명 교체** | GitHub 공개 저장소에 노출됐던 DB 비밀번호·Kakao REST API 키 | 코드에서 제거해도 **커밋 히스토리에 남아있음**. 저장소 비공개 전환 + 두 값 모두 교체 필요 |
| 3. **비밀번호 평문 저장** | `AuthProfileMapper.xml`이 `user_password = #{userPassword}` 로 평문 비교, 저장도 평문 | DB 유출 시 전 직원 비밀번호 즉시 노출. BCrypt 도입 + 마이그레이션 정책 결정 필요 |
| 4. **Cloudflare→원본 구간 평문 HTTP** | SSL/TLS 모드가 Flexible — 브라우저↔Cloudflare만 HTTPS, Cloudflare↔사무실(8080)은 평문 | 그 구간에서 로그인 비밀번호가 평문으로 흐름. Cloudflare Tunnel 또는 Full 모드+원본 인증서로 해결 |
| 5. **업로드 파일 형식 검증 없음** | 전 업로드 경로에 확장자·매직바이트 검증 없음(크기 제한만 존재) | 실행 파일 등 임의 형식 업로드 가능 |
| 6. **문서 다운로드 인가 없음** | 매장·물건·활동첨부 다운로드는 호출자 신원조차 받지 않음 | `storeIdx`/`docIdx` 순회로 전체 문서 수집 가능(위 1번과 결합 시 심각) |

### 20. 2026-07-31 — 토큰 인증 도입 (§19-1 대응)
> 외부 라이브러리 없이 JDK HMAC-SHA256 서명 토큰을 사용. `spring-boot-starter-security` 를 켜면 전 엔드포인트에 기본 차단 체인이 걸려 파급이 크므로 의도적으로 쓰지 않았다.

| `auth/token/AuthTokenService.java` (신규) | 로그인 시 `base64(userId\|발급\|만료).base64(HMAC서명)` 토큰 발급, 검증 시 상수시간 비교 + 만료 확인 | 무상태라 DB 테이블·마이그레이션 불필요, 서명 키만 고정하면 재시작에도 로그인 유지 |
| `auth/token/AuthTokenFilter.java` (신규) | ① 허용목록(`/auth/login`, `/health`) 외 전 요청에 유효 토큰 요구 ② **요청의 `userId` 파라미터가 토큰 주인과 다르면 403** | ②가 핵심 — 45개 엔드포인트를 하나하나 고치지 않고도 "남의 ID 를 적어 넣어 타인 행세"를 전 구간에서 차단 |
| 〃 | 브라우저가 직접 여는 요청(WebSocket 핸드셰이크, `<img src>`·새 탭 다운로드)은 헤더를 못 실으므로 `/ws/chat` 과 `/download` 경로에 한해 `?token=` 허용 | 그 외 경로는 헤더만 인정 — 토큰이 URL·접근로그에 남는 범위 최소화 |
| `common/HealthController.java` (신규) | 인증 불필요한 `/health` | 운영 스크립트가 업무 엔드포인트(`/stores`, `/codes`)로 헬스체크하던 것을 대체 |
| `auth/dto/AuthProfileDto`, `AuthService` | 로그인 응답에 `accessToken` 추가 | |
| `scripts/health-check.ps1`, `status.ps1` (라이브+템플릿) | 헬스체크 대상을 `/api/health` 로 변경 | 인증 도입 후 기존 체크가 401 로 실패하는 것 방지 |
| `config/backend.env.example` (라이브+템플릿) | `AUTH_TOKEN_SECRET`, `AUTH_TOKEN_VALIDITY_HOURS`, `KAKAO_REST_API_KEY` 항목 추가 | |
| **Flutter** `core/auth/auth_token_store.dart` (신규) | 토큰 보관소 | |
| `core/api/api_client.dart` | Dio 인터셉터가 모든 요청에 `Authorization: Bearer` 자동 첨부 | 단일 지점이라 화면 코드 수정 불필요 |
| `core/auth/auth_provider.dart` | `_applyProfile()` 도입 — 프로필 대입 시 토큰 동기화를 강제(로그인·자동로그인·복원·로그아웃·권한갱신 5개 경로 일괄) | 한 경로라도 누락되면 전 API 가 401 이 되므로 대입을 한 곳으로 모음 |
| `core/auth/auth_profile.dart` | `accessToken` 필드 추가(SharedPreferences 저장분에 포함되어 새로고침 후에도 유지) | |
| `core/chat/websocket_chat_service.dart` | WS URL·첨부 URL 에 토큰 부착 | |

**하위호환 설계**: 신규 클라이언트 + 구버전 백엔드 = 정상 동작(토큰 미발급 → 헤더 미첨부 → 구버전은 애초에 검사 안 함). 실제로 라이브에서 로그인 200 확인. 반대로 **구버전 클라이언트 + 신규 백엔드 = 전원 401**이므로 반드시 웹을 먼저 배포하고 백엔드를 나중에 배포할 것.

**검증**: 백엔드 `mvn package` 통과(JAR 생성), Flutter `analyze` 경고 0(잔여 info 2건은 기존 스타일 지적), 웹 빌드 통과.

**토큰 로직 실행 검증** (8/8 통과): 정상 발급·검증 / null·빈값·형식불량 거부 / **서명 변조 거부** / **payload 를 admin 으로 위조 시 거부** / 다른 서명키 토큰 거부 / 만료 토큰 거부.

### 21. 2026-07-31 — 비밀번호 BCrypt 전환 + 전체 초기화 (§19-3 대응)
| `pom.xml` | `spring-security-crypto` 만 추가 | `spring-boot-starter-security` 는 전 엔드포인트 차단 체인이 자동으로 붙어 파급이 크므로 암호화 모듈만 사용 |
| `auth/password/PasswordHasher.java` (신규) | BCrypt 해싱·검증. 저장값이 BCrypt 형식이 아니면 평문 비교로 폴백하고 `needsRehash` 를 알림 | 마이그레이션 전에 남아 있을 수 있는 평문 계정도 로그인은 되게 하되, 성공 시점에 즉시 해시로 승격 |
| `AuthProfileMapper.xml` / `.java` | `selectByUserIdAndPassword`(SQL 동등비교) **삭제** → `selectPasswordHash` + 앱단 BCrypt 검증으로 교체. `selectPwdResetYn`, `updatePasswordHash` 추가 | 평문 비교 전제를 제거 |
| `AuthService.login` | 해시 조회 → BCrypt 검증 → 프로필 조회 순으로 변경. 평문 잔재는 로그인 성공 시 자동 해시 전환 | |
| `AuthService.changePassword` (신규) | 현재 비밀번호 확인 + 8자 이상 + 기존과 동일값 거부 → 해시 저장 + 강제 플래그 해제 | |
| `AuthController` `POST /auth/change-password` (신규) | **대상 사용자를 요청에서 받지 않고 토큰에서 판단** | 남의 비밀번호를 바꾸는 경로 자체를 만들지 않기 위함 |
| `MstService`, `OwnerUserService`, `AuthService.updateUserProfile` | 계정 생성·수정 시 비밀번호를 항상 해시 저장(평문 저장 경로 4곳 전부) | |
| `deploy/db/migrations/20260731_user_password_bcrypt_reset.sql` (신규) | `pwd_reset_yn` 컬럼 추가 + 전 계정 비밀번호를 초기값 해시로 재설정 | **되돌릴 수 없음 — 실행 전 백업 필수.** 초기 비밀번호 `yeokjeon1234!` (해시는 생성 후 검증까지 확인) |
| **Flutter** `core/auth/change_password_view.dart` (신규) | 비밀번호 변경 화면(현재/새/확인, 8자 검증, 로그아웃 탈출구) | |
| `core/router/app_router.dart` | `mustChangePassword` 면 어떤 경로로 가도 `/change-password` 로 강제 이동, 변경 완료 시 원래 화면으로 복귀. 셸(사이드바) **밖**에 배치 | 변경 전에 메뉴로 빠져나가지 못하게 |
| `core/auth/auth_provider.dart` | `markPasswordChanged()` — 플래그 해제 + 저장분 갱신 + **저장된 자동로그인 비밀번호 삭제** | 비밀번호가 바뀌었으므로 기존 저장 비밀번호는 무효 |

### 22. 배포 준비 상태 (2026-07-31 기준)
- 릴리즈 패키지: `C:\y-on\release\yon-field-server-20260802-161634` (backend JAR + web + 마이그레이션 SQL)
- 웹은 동기화 폴더(`C:\y-on\yon-field-server\web`)에 반영 완료. **단, 라이브 전파가 지연 중** — 배포 전 `curl -s https://test.yeokjeon.com/main.dart.js | grep -c "Bearer "` 가 1 이 나오는지 확인할 것(0 이면 아직 미전파).
- **배포 순서**: ① 웹 전파 확인 → ② DB 백업 → ③ 마이그레이션 SQL 실행 → ④ `backend.env` 에 `AUTH_TOKEN_SECRET`·`KAKAO_REST_API_KEY` 추가 → ⑤ `update-app.ps1` 로 백엔드 교체.
- 순서를 지키지 않고 백엔드를 먼저 올리면 구버전 클라이언트 사용자가 전원 401 이 된다.
- **비밀번호 초기화 마이그레이션은 보류**(사용자 요청) — 계정 정보를 받아 초기 세팅 예정. `20260731_user_password_bcrypt_reset.sql` 은 실행하지 말 것. BCrypt 코드 자체는 평문 폴백이 있어 마이그레이션 없이도 동작하며, 로그인 성공 시 자동으로 해시로 승격된다.

## 2026-08-02 — 전 화면 CRUD 점검

### 23. 점검 방법과 범위
라이브(`test.yeokjeon.com`, 구버전 JAR = 인증 없음) 대상으로 REST API 를 직접 호출해 조회·등록·수정·삭제를 전수 확인. 생성한 테스트 데이터는 모두 정리했다(가맹점 1046, 사원 104, 예비창업자 7, 체크리스트 4, 게시글 10 삭제 / 폴더 4 는 비활성화).

**정상 확인**: 공통코드·메뉴·가맹점·예비창업자·물건·영업지역·사원·부서·가맹점주·체크리스트·사용기록·게시판(폴더/글)·알림·메신저·활동·활동계획·전자결재 조회 전부 200. 등록·수정은 사원(POST/PUT), 예비창업자(POST 201/PUT), 체크리스트(POST 201/PUT), 게시판 폴더·글(POST/PUT), 가맹점(POST 201/PUT + 이력조회)까지 확인하고 **재조회로 실제 반영까지 검증**. 입력값 검증(필수값 누락 시 400 + 사유)도 정상 동작.

### 24. 발견·수정한 결함
| `exception/GlobalExceptionHandler.java` | 최종 catch 가 `"서버 오류: " + ex.getMessage()` 로 **예외 원문을 그대로 클라이언트에 반환**하던 것을 일반 문구로 변경(상세는 서버 로그에만) | 게시판 폴더 삭제 실패 시 **PostgreSQL 오류 전문 — 실행 SQL·테이블명·제약조건명까지 — 이 응답에 그대로 노출**되고 있었음(정보 노출). 사용자에겐 뜻 모를 오류로 보임 |
| 〃 | `MissingServletRequestParameter`(필수 파라미터 누락) → 500 이던 것을 **400 + "필수 항목이 빠졌습니다: xxx"** 로 처리. 타입 불일치·JSON 파싱 실패도 400 으로 추가 | 클라이언트 실수가 "서버 오류"로 표시돼 원인 파악이 어려웠음 |
| 〃 | `DataIntegrityViolationException`(FK 위반 등) → **409 + "연결된 데이터가 있어 처리할 수 없습니다"** | 하위 데이터가 있는 상위 항목 삭제 시 전 화면 공통으로 적용 |
| `mapper/board/BbsFolderMapper.xml` `deleteFolder` | 물리 삭제(`DELETE`) → **소프트 삭제(`use_yn='N'`)** | **실사용 버그**: 게시글은 소프트 삭제(행 유지)인데 폴더 삭제 가드는 `deleted_yn=false` 인 글만 세어 0 으로 통과 → 실제 DELETE 에서 FK 위반. 결과적으로 **글을 지운 "빈" 폴더는 영영 삭제 불가**였다. 목록 조회가 `use_yn='Y'` 만 보므로 소프트 삭제로 바꾸면 즉시 사라지고 데이터도 보존된다 |
| `test/mst001_csv_test.dart` (신규) | CSV 다운로드→업로드 왕복 테스트 5종(BOM·CRLF·쉼표/따옴표 이스케이프·로그인ID 보존·ID 없는 사원) | 보고된 "CSV 일괄 업로드 미반영" 원인 규명용 — **전부 통과**하여 CSV 인코딩/파싱은 정상임을 확인 |

### 25. CSV 일괄 업로드 미반영 — 현재까지 확인된 사실
- CSV 생성·파싱 로직: 단위테스트 5종 통과(정상)
- 매칭 키: 실제 사원 70명 **전원 로그인ID 보유, 중복 없음** → 매칭 실패 시나리오 아님
- 백엔드 `PUT /users/{idx}`: 200 + **재조회로 실제 저장 확인**(이름·전화·이메일·직급·태그여부 모두 반영)
- 즉 CSV·매칭·서버 저장 3개 층은 모두 정상. **재현 실패** — 원인 미확정
- 조치: 업로드 실패 시 원인을 `debugPrint` 로 남기도록 이미 수정해 둠. 재발 시 화면 하단 문구(수정 N건/못찾음 N건/실패 N건)를 확인하면 층을 특정할 수 있다

## 2026-08-02 — 실서버 반영

### 26. 레실리오 동기화 단절 (배포가 막혔던 원인)
7/24 이후 `C:\y-on` 의 레실리오 동기화가 **양방향 모두 끊겨 있었다.** 그동안 "서버에 반영했다"고 판단한 7/24 이후 작업분(전자결재 사이드바 재구성, 로고, 인증 등)은 실제로는 서버에 도달하지 않았다.

- 판정 근거: 동기화 폴더에 마커 파일 생성 → 30초간 라이브에서 조회 불가 / 7/25~8/1 사이 서버발 변경 수신 기록도 0건 / Cloudflare 캐시 아님(`cf-cache-status: BYPASS`, 캐시 우회해도 구파일) / 라이브 파일 날짜 7/24
- **교훈**: 배포 확인을 `HTTP 200` 이나 `Last-Modified` 헤더로 하면 안 된다(둘 다 오탐이었다). **응답 본문에 이번 변경분 고유 문자열이 있는지**로 확인할 것.
  예) `curl -s https://test.yeokjeon.com/main.dart.js | grep -c "Bearer "`
- 사용자가 재연결한 뒤 마커 파일이 5초 내 도달하는 것을 확인하고 배포 재개.
- **주의**: 재연결 시 서버 쪽 상태가 우선 반영되어, 로컬에만 있던 릴리즈 폴더와 `backend.env` 수정이 되돌려졌다. 재연결 직후에는 스테이징을 다시 확인할 것.

### 27. 반영 내역
| 웹 (`C:\y-on\yon-field-server\web`) | 신규 빌드 배포 — **라이브 반영 확인 완료** | 검증: 본문에 인증 코드 존재 / 크기 6,152,329 일치 / 실제 로그인 200. 구버전 백엔드와 호환되도록 만들어 백엔드 배포 전에도 정상 동작 |
| `caddy/Caddyfile.http` | 보안 헤더 + 루트 랜딩 라우팅 | **Caddy 재시작 후 적용** |
| `scripts/health-check.ps1`, `status.ps1` | 헬스체크 대상을 `/api/health` 로 | 인증 도입 후 기존 체크가 401 로 실패하는 것 방지 |
| `config/backend.env` | `AUTH_TOKEN_SECRET`, `AUTH_TOKEN_VALIDITY_HOURS`, `KAKAO_REST_API_KEY` 추가 | |
| `release/yon-field-server-20260802-2230` | 백엔드 JAR(인증·BCrypt·예외처리·게시판 폴더 수정 포함) + web + config | **백엔드 교체·재시작은 서버 PC 에서 실행 필요**(별도 기계라 원격에서 프로세스 재시작 불가) |

서버에서 실행할 명령:
```
powershell -ExecutionPolicy Bypass -File C:\y-on\yon-field-server\scripts\backup-db.ps1
powershell -ExecutionPolicy Bypass -File C:\y-on\yon-field-server\scripts\update-app.ps1 -ReleasePath C:\y-on\release\yon-field-server-20260802-2230
powershell -ExecutionPolicy Bypass -File C:\y-on\yon-field-server\scripts\stop-web.ps1
powershell -ExecutionPolicy Bypass -File C:\y-on\yon-field-server\scripts\start-web.ps1
```
백엔드 교체 시점부터 인증이 켜지므로 **이미 앱을 열어둔 사용자는 새로고침 후 재로그인**해야 한다. 기존 비밀번호 그대로 사용 가능(평문 폴백 → 로그인 성공 시 자동 해시 전환).

## 2026-08-02 — 보안 재점검 (인증 도입 후 2차)

토큰 인증을 넣은 뒤 다시 훑었다. **"로그인했는가"는 막았지만 "그 일을 할 자격이 있는가"는 아직 아무 데서도 안 보고 있었다**는 것이 이번 점검의 핵심 결과다.

### 28. 내가 만든 결함 — 서명키 로그 노출 (치명)
| `auth/token/AuthTokenService.java` | 자동 생성한 HMAC 서명키를 16진수로 **WARN 로그에 찍던 것을 제거**. 아울러 설정 키가 32자 미만이면 **기동을 중단**하도록 검증 추가 | `auth.token.secret` 기본값이 빈 문자열이라 **환경변수를 안 넣은 배포에서는 이 경로가 기본 동작**이었다. 로그 파일을 볼 수 있는 사람이면 누구나 `admin` 을 포함한 **임의 사용자의 토큰을 위조**할 수 있었다. 인증을 넣으면서 같이 들어간 결함이라 인증 도입 전보다 오히려 위험했다 |

토큰 검증 단위테스트 9종(서명 변조·payload 위조·다른 키·만료·짧은 키 거부 포함) **전부 통과**.

### 29. 권한 검사 부재 — 권한 상승 경로 (치명)
토큰 필터는 신분(누구인가)만 확인하고 자격(무엇을 할 수 있는가)은 보지 않았다. 그래서 **로그인만 하면 말단 사원도** 아래를 할 수 있었다.

| 신규 `auth/access/MenuAccessGuard.java`, `AccessDeniedException.java`, `MenuCodes.java` | 기존 메뉴권한 모델(`admin_yn` 또는 `user_menu_auth`)을 그대로 재사용하는 공통 가드. 별도 역할 개념을 새로 만들지 않아 **메뉴권한 관리 화면에서 준 권한이 그대로 API 에 적용**된다 | |
| `master/controller/MstController.java` POST/PUT/DELETE `/users` | 사원관리(mst001) 권한 요구 | **전체 탈취 경로였다** — 아무나 남의 비밀번호를 재설정하거나, 자기 로그인ID 를 `admin` 으로 바꿔 슈퍼관리자가 될 수 있었다 |
| `master/controller/MenuPermissionController.java` PUT `/users/{idx}/menu-permissions` | 메뉴권한 관리(mst003) 권한 요구 | 자기 자신에게 전 메뉴 권한을 부여하는 **직접적인 권한 상승** |
| `master/controller/OwnerUserController.java` POST/PUT/DELETE | 가맹점주 관리(mst006) 권한 요구 | 가맹점주도 `user_mst` 계정(비밀번호 포함)이라 위와 동일 |
| `exception/GlobalExceptionHandler.java` | `AccessDeniedException` → **403** | |

### 30. 신분 위조 — 필터가 못 보는 경로 (높음)
필터는 `?userId=` 쿼리 파라미터만 대조한다. **JSON 본문에 담긴 신분과 숫자 키(userIdx)는 걸러지지 않았다.**

| `master/controller/UsageLogController.java` POST `/usage-logs/menu`, `/tag` | 본문 `userId` 가 토큰 주인과 다르면 403 | **감사기록 위조** — 남의 이름으로 메뉴 사용·가맹점 출입(GPS 포함) 기록을 남길 수 있었다 |
| 〃 GET `/usage-logs` | 사용기록 조회(mst005) 권한 요구 | 전 직원의 메뉴 이력과 **출입 태그 GPS 좌표**가 누구에게나 열려 있었다. 사실상 위치 추적 자료 |
| `active/controller/ActivityPlanController.java` (`viewerUserIdx` 4곳) | `MenuAccessGuard.ensureSelfUserIdx` 로 토큰 주인의 userIdx 와 대조 | **팀 캘린더 열람 권한 우회** — 남의 번호를 넣으면 볼 권한이 없는 팀 일정이 열렸다. 클라이언트는 항상 본인 `profile.userIdx` 를 보내므로 화면 동작에는 영향 없음(확인함) |
| 〃 PUT `/users/{idx}/team-view-permissions` | 본인 또는 mst003 권한자만 | 아무나 **자기 자신을 남의 열람 허용 목록에 추가**해 일정을 들여다볼 수 있었다. mst003 화면에서 관리자가 대신 편집하는 정상 경로가 있어 그쪽은 허용 |
| 〃 `createdBy` / `grantedBy` | 클라이언트 값 대신 **토큰 주인으로 덮어씀** | 작성자 기록 위조 방지 |

### 31. 로그인 무차별 대입 방어 (높음)
| 신규 `auth/login/LoginAttemptGuard.java` + `AuthController` | 계정별 연속 실패 10회 → **10분 잠금**(429). 30분간 실패가 없으면 카운트 초기화, 로그인 성공 시 즉시 해제 | 로그인은 토큰 없이 열린 유일한 쓰기 경로인데 시도 횟수 제한이 **전혀 없었다**. 인터넷에 열린 주소로 초당 수백 번 비밀번호를 넣어볼 수 있었고, 계정 하나가 뚫리면 그 계정의 메뉴 권한 전체가 넘어간다 |

### 32. 비밀번호를 바꿔도 기존 토큰이 살아있던 문제 (중간)
| 신규 `auth/token/TokenInvalidationRegistry.java` | 사용자별 "이 시각 이전 발급분은 무효" 기준선. `AuthTokenFilter` 가 매 요청 확인 | 무상태 토큰이라 그냥 두면 **이미 새어 나간 토큰이 만료(12시간)까지 계속 통했다.** "누가 들어온 것 같아서 비밀번호를 바꿨다"는 대응이 아무 효과가 없다는 뜻 |
| `AuthController` 비밀번호 변경 | 기존 토큰 전부 무효화 + **새 토큰 재발급해 응답에 실어줌**(`AuthTokenDto`) | 무효화만 하면 바꾼 본인이 직후에 전부 401 로 튕긴다 |
| `AuthTokenService.issue(userId, issuedAt)` 오버로드 | 기준선보다 확실히 뒤(+1ms)인 발급시각 지정 | 무효화와 재발급이 같은 밀리초에 일어나면 방금 발급한 토큰까지 무효 처리되는 경계 버그를 원천 차단 |
| `MstController` 사용자 수정/삭제 | 관리자가 비밀번호를 재설정하거나 계정을 지우면 그 계정 토큰도 함께 끊음 | 안 끊으면 잠근 계정으로 만료까지 계속 접속 가능 |
| 클라이언트 `auth_api_service.dart`, `auth_provider.dart`, `auth_profile.dart`, `change_password_view.dart` | 변경 응답의 재발급 토큰을 세션에 반영(`afterPasswordChange`). `markPasswordChanged` 가 **강제 변경이 아닐 때도 동작**하도록 수정 | 기존 코드는 `mustChangePassword` 가 false 면 조기 반환해서, 설정 화면에서 자발적으로 바꾼 경우 토큰 교체가 안 됐다 |

### 33. 확인했으나 조치하지 않은 것
- **문서 다운로드 IDOR**: 파일 경로는 DB 값으로만 만들어지고 `storageRoot` 하위로 `normalize()` 되므로 **경로 조작으로 임의 파일을 읽을 수는 없다.** 남는 것은 "로그인한 직원이 다른 가맹점 문서를 받을 수 있다"인데, 가맹점 목록·상세 자체가 전 직원 공개인 현 모델과 일관된다. 문서만 따로 막으면 화면이 깨지므로 보류
- **`svId` / `relUserId` 목록 필터**(`ActController`): 같은 목록을 아무 필터 없이 반환하는 API(`by-suggestions`, `by-check`)가 이미 있어 데이터가 구획되어 있지 않다. 이것만 막아도 실익이 없어 보류
- **잠금·무효화 기록이 메모리 보관**: 백엔드가 단일 인스턴스라 충분하다. 재시작하면 사라지지만 그 시점엔 비밀번호가 이미 바뀌어 있어 새 로그인은 새 비밀번호를 요구한다

### 34. 검증 결과
새 JAR 을 **실제로 기동**해(포트 3099) 확인했다. 컴파일만 하고 "됐다"고 하지 않기 위함.

- **기동 성공** — 신규 빈(`MenuAccessGuard` / `TokenInvalidationRegistry` / `LoginAttemptGuard`) 배선 포함 34초 만에 정상 기동
- **환경변수 주입 확인** — `AUTH_TOKEN_SECRET` 을 넣으면 "미설정" 경고가 사라짐(= 설정 키 경로를 탄다)
- 실제 HTTP 호출 결과:

| `GET /api/health` (토큰 없음) | **200** | 공개 경로 정상 |
| `GET /api/users` (토큰 없음) | **401** | 이전에는 200 이었다 |
| `GET /api/users` (엉터리 토큰) | **401** | |
| `GET /api/usage-logs` (토큰 없음) | **401** | |
| `GET /api/activity-plans/month` (토큰 없음) | **401** | |
| `GET /api/users?userId=남의ID` (유효 토큰) | **403** | 신분 사칭 차단 |
| `POST /api/usage-logs/menu` 본문 `userId=victim` | **403** `"다른 사용자의 이름으로 기록할 수 없습니다."` | 감사기록 위조 차단 + 403 매핑 동시 확인 |
| 〃 본문 `userId=본인` | 필터·가드 통과 | |

- 단위테스트: 토큰 9종(서명 변조·payload 위조·다른 키·만료·짧은 키 거부) + 잠금/무효화 13종 = **22종 전부 통과**
  - 잠금 쪽은 "정상 사용자를 막지 않는지"도 확인했다 — 9회까지는 통과, 로그인 성공 시 즉시 초기화, 다른 계정 무영향
- `flutter analyze`: 에러 0 (기존 info 2건만 잔존)
- **라이브 웹 반영 확인** — `main.dart.js` 6,152,864 바이트(직전 6,152,329)가 실제 응답으로 내려오는 것을 확인

**아직 라이브는 구버전 JAR** 이다. 방금 `GET /api/users` 를 토큰 없이 불러 **200** 이 떨어지는 것을 확인했다 — 즉 현재 서버는 인증이 없는 상태다. 아래 3번을 실행해야 위 차단이 실제로 켜진다.

**배포 후 주의 — 권한 검사는 "막는 쪽"으로 실패한다.**
`mst001` `mst003` `mst005` `mst006` 네 코드는 `menu_mst` 에 실재함을 확인했다(전체 19개 메뉴). 다만 이제부터는 **해당 메뉴 권한이 실제로 부여된 사람만** 그 화면의 등록·수정·삭제가 된다. 슈퍼관리자(`admin_yn='Y'`)는 전부 통과하므로, 배포 후 관리자 계정으로 로그인해 **메뉴권한 관리 화면에서 담당자에게 권한을 주면 된다.** 그 전까지 비관리자에게는 "이 작업을 수행할 권한이 없습니다."(403)가 뜬다 — 오류가 아니라 의도된 동작이다.

## 2026-08-03 — 백엔드 배포 후 로그인 전면 장애 (내가 낸 사고)

### 35-A. 무슨 일이 있었나
새 JAR 배포 직후 **전 사용자가 로그인할 수 없었다.** 사용자에게는 "가맹점 관리·개발 관리 데이터가 사라진 것처럼" 보였다.

**먼저 확인한 것 — 데이터는 지워지지 않았다.** 배포 7초 전 백업(`yj_db_test-20260803-001752.dump`) 기준 가맹점 1,040 / 영업지역 1,028 / 물건 112 / 예비창업자 5 / 사원 71행이 그대로 있고, `update-app.ps1` 과 `start-*.ps1` 은 SQL 을 한 줄도 실행하지 않으며 `ddl-auto: none` 이다(`create-drop` 인 dev 프로파일은 활성화되지 않음). 즉 **스키마·데이터를 건드릴 경로 자체가 없다.**

**진짜 원인 — 로그인이 막혀 화면이 빈 것이었다.**
1. 실 DB `user_mst` 에 **`pwd_reset_yn` 컬럼이 없다.** 그 컬럼은 실행하지 않기로 한 `20260731_user_password_bcrypt_reset.sql` 에서만 추가된다
2. 실 DB 비밀번호 **71개가 전부 평문**이라 로그인마다 "평문 → BCrypt" 전환 UPDATE 가 돈다
3. 그 UPDATE 가 `pwd_reset_yn` 을 같이 SET 하고 있었다 → 없는 컬럼 → SQLException
4. `AuthService.login()` 의 넓은 `catch` 가 이를 삼키고 `null` 반환 → 컨트롤러가 **401**

비밀번호는 맞는데 401 이 떨어지니, 원인을 "비밀번호 틀림"으로 오해하기 딱 좋은 형태였다.

### 35-B. 고친 내용
| `mapper/auth/AuthProfileMapper.xml` | `updatePasswordHash` 에서 `pwd_reset_yn` 을 분리해 별도 `updatePwdResetYn` 으로 뺌 | 한 문장에 묶어 두면 선택 컬럼 하나 때문에 비밀번호 갱신 전체가 실패한다 |
| `AuthService.login()` | 해시 전환을 개별 try/catch 로 감쌈. 슈퍼관리자 판정도 동일 처리 | **부가 정보(사용기록·권한·관리자표시·강제변경표시) 실패가 로그인을 막으면 안 된다.** 비밀번호가 맞고 사용자를 찾았으면 로그인은 성립한 것 |
| `AuthService.changePassword()` / `AuthController` | 플래그 해제를 `clearPasswordResetFlag()` 로 빼서 **트랜잭션 밖**에서 호출 | `@Transactional` 안에서는 try/catch 로 잡아도 롤백 표시가 남아 **이미 성공한 비밀번호 변경까지 되돌아간다** |
| `MenuPermissionService.isSuperAdmin()` | 설정값이 비어 있으면 NPE 대신 false | 로그인 경로에서 호출되므로 절대 터지면 안 된다 |
| `scripts/health-check.ps1` | `admin`/`admin123` 실제 로그인 시도를 제거하고, 인증 필요 경로가 **401 을 주는지**로 게이트웨이 확인 | ① 매번 `[FAIL]` 로 보여 진짜 장애와 구분이 안 됐다 ② 새 잠금 정책 때문에 **반복 실행하면 admin 계정이 10분 잠긴다** |

### 35-C. 클라이언트 — 세션이 끊겨도 "빈 화면"만 보이던 문제
서버가 401 을 줘도 앱은 아무 대응이 없어, 로그인 상태로 남은 채 목록만 비어 보였다. 이번 사고를 사용자가 "데이터 소실"로 인식한 직접적인 이유다.

| `auth_token_store.dart` | 401 을 알리는 `sessionExpired` 신호 추가 | |
| `api_client.dart` | `onError` 에서 401 감지 시 신호 발생(로그인 요청은 제외) | |
| `auth_provider.dart` | 신호를 받으면 로그아웃 → 라우터가 로그인 화면으로 | |
| 〃 세션 복원 | **토큰 없는 저장 세션은 폐기**하고 재로그인 유도 | 인증 도입 전에 저장된 세션이 그대로 복원되면 "로그인됨"으로 보이지만 모든 조회가 401 이라 전 화면이 빈다 |

### 35-D. 검증
- **회귀 테스트 신설**(`LoginRegressionCheck`): 실 DB 를 흉내낸 매퍼(평문 비밀번호 + `pwd_reset_yn` 없음 + 해시전환 UPDATE 가 예외)로 `AuthService.login()` 을 직접 호출 — **5종 통과**
  - 해시 전환 UPDATE 가 실패해도 로그인 성공 / 해시 전환 경로를 실제로 탐 / 토큰 발급됨 / 발급 토큰이 검증 통과 / **틀린 비밀번호는 여전히 거부**
  - 이 테스트를 처음 돌렸을 때 `isSuperAdmin` 의 같은 종류 취약점이 추가로 드러나 함께 고쳤다
- **실 스키마 대조**: 백업 덤프에서 실 스키마(28개 테이블)를 추출해, 이번에 손댄 매퍼 SQL 이 참조하는 컬럼이 실제로 존재하는지 기계적으로 검사. 남은 불일치는 `pwd_reset_yn` 하나이며 이제 전부 방어 경로다
  - **이 대조를 처음부터 했으면 사고를 막을 수 있었다.** 앞으로 매퍼 SQL 을 고치면 이 검사를 먼저 돌린다

### 35-E. 추가 구조 개선 — 오류가 "비밀번호 틀림"으로 위장되지 않게
`login()` 전체를 감싸던 `try/catch → return null` 을 제거했다. 이제 **비밀번호 불일치만 401**이고, 예상치 못한 서버 오류는 **500** 으로 드러난다.

이번 사고가 오래 안 잡힌 이유가 정확히 이것이다 — DB 스키마 문제인데 화면에는 "아이디 또는 비밀번호가 일치하지 않습니다"가 떠서, 원인을 찾을 단서가 응답에서 통째로 사라졌다. 부가 정보 단계는 개별 try/catch 로 여전히 보호되므로, 로그인이 부가 기능 때문에 실패하는 일도 없다.

회귀 테스트에 이 항목도 넣었다 — "핵심 경로 오류는 401 로 위장되지 않고 드러난다" (총 **6종 통과**).

### 35-F. 배운 것
- 로그인처럼 **모든 것의 관문이 되는 경로**에서는 넓은 `try/catch` + `return null` 이 특히 위험하다. 실패 원인이 통째로 지워지고 "인증 실패"로 둔갑한다
- 선택 컬럼(나중에 추가된 컬럼)을 필수 UPDATE 문에 섞으면 안 된다
- `@Transactional` 안의 try/catch 는 예외를 삼켜도 **롤백을 막지 못한다**

### 35-G. 부수 정리
- `.gitignore` 에 `.sync/` 추가 — Resilio 메타데이터(공유 폴더 식별자·ACL)가 추적 대상에 잡혀 있었다. 저장소가 공개 상태라 더 올라가면 안 된다
- `config\backend.env` 의 깨진 한글 주석 2줄을 영문으로 교체(값은 그대로). 인코딩이 섞여 읽을 수 없는 상태였다

### 35-H. 관리자 계정 초기 설정
요청에 따라 `admin` 계정 비밀번호를 지정했다. **DB 는 서버 PC 에 있어 내가 직접 실행할 수 없으므로 SQL 스크립트로 만들어 두었다.**

- 파일: `C:\y-on\yon-field-server\db\900_admin_account_setup.sql` (레실리오로 서버에 동기화됨)
- **공개 저장소에는 넣지 않았다** — `C:\y-on_share\y-on` 이 아니라 서버 폴더에만 둔다. 저장소에 비밀번호·해시가 남지 않는 것을 확인했다
- 평문이 아니라 **BCrypt 해시**를 넣는다. 평문으로도 로그인은 되지만(과도기 폴백) DB 에 비밀번호가 그대로 남는다
- 해시는 앱이 실제로 쓰는 `PasswordHasher` 로 생성 — 형식 불일치로 로그인이 안 되는 사고를 막기 위함
- `admin` 행은 이미 존재한다(userIdx=5, `admin_yn='Y'`). 비밀번호만 갱신하며 다른 계정은 건드리지 않는다

검증 4종 통과: SQL 파일에 적힌 해시로 지정한 비밀번호가 검증됨 / 다른 비밀번호는 거부됨 / BCrypt 로 인식됨(재해시 대상 아님) / 파일에 평문이 없음.

**⚠ 보안상 짚어둘 점**: 지정된 값이 `config\backend.env` 의 **DB 비밀번호와 동일**하고, 그 값은 공개 상태인 GitHub 저장소 이력에 이미 남아 있다. 즉 유출된 문자열 하나로 **DB 와 ERP 관리자 계정이 동시에** 열린다. 요청대로 설정했으나, 저장소 비공개 전환 후 **DB 비밀번호와 관리자 비밀번호를 서로 다른 값으로 각각 교체**할 것을 권한다. 교체는 앱에 로그인해 비밀번호 변경 화면에서 하면 되고(그 경로는 BCrypt 로 저장된다), 이 SQL 을 다시 쓸 필요는 없다.

## 2026-08-03 — 도메인 변경 (test.yeokjeon.com → on / on-test)

### 35-I. 변경 방침
- **운영**: `on.yeokjeon.com`
- **테스트**: `on-test.yeokjeon.com`
- **구 `test.yeokjeon.com` 은 폐기**

처음 요청은 `on.test.yeokjeon.com` 이었으나, Cloudflare 무료 Universal SSL 인증서는 `yeokjeon.com` 과 **`*.yeokjeon.com` 까지만** 커버한다. `on.test.yeokjeon.com` 은 점이 하나 더 들어간 3단계 서브도메인이라 인증서 범위 밖이고, 쓰려면 Advanced Certificate Manager(유료)가 필요하다. 이 점을 알려드리고 `on` / `on-test` 로 확정했다.

### 35-J. 코드에서 바꾼 곳
| `backend/config/WebConfig.java` | CORS 허용 출처 | `on.yeokjeon.com`, `on-test.yeokjeon.com` 추가 / 구 주소 제거 |
| `backend/chat/ws/ChatWebSocketConfig.java` | WebSocket 허용 출처 | 〃 (CORS 와 동일하게 유지해야 메신저가 붙는다) |
| `backend/eap/service/EapConnectionService.java` | 다우오피스 콜백 기본값 | `https://on.yeokjeon.com/api/eap/status`. 테스트 서버는 `DAOU_CALLBACK_URL` 로 덮어쓸 것 |
| `app_flutter/lib/core/api/api_base_url_config.dart` | Android 앱 기본 API 주소 + 카카오 지도 origin | 기본값 3곳 |
| `app_flutter/dart_defines.{local,local.example,company-lan}.json` | 빌드 정의 | |
| `deploy/server/Caddyfile`, `cloudflared-config.yml.example` | 참고용 설정 | |
| `deploy/server/README.md`, `deploy/package/field-server/README.md` | 문서 | |

**현장 서버의 실제 Caddy 설정(`Caddyfile.http`)은 손댈 게 없다.** 호스트명 없이 `:8080` 으로만 수신하고, 호스트명 라우팅은 Cloudflare Origin Rule 이 담당하기 때문이다.

**웹 번들도 도메인을 타지 않는다.** `API_BASE_URL=/api`(같은 출처)로 빌드하므로 어떤 도메인에 올려도 그대로 동작한다.

이걸 추측이 아니라 **실측으로 확인했다** — 도메인 변경 후 다시 빌드한 `main.dart.js` 의 SHA256 이 현재 라이브 파일과 **완전히 동일**했다(`2CB42429...`). 도메인 문자열이 애초에 번들에 들어가지 않는다는 뜻이다(카카오 지도 origin 은 모바일 전용 코드라 웹 빌드에서 트리셰이킹됨). 따라서 **웹은 재배포할 필요가 없고**, 이번 변경으로 실제 영향받는 산출물은 **백엔드 JAR 과 Android APK 뿐**이다.

### 35-K. 서버 밖에서 해야 하는 일 (이게 본체다)
1. **Cloudflare DNS** — `on`, `on-test` A/CNAME 레코드를 기존 `test` 와 같은 대상으로 추가(프록시 ON)
2. **Cloudflare Origin Rule** — 기존 규칙이 `Hostname equals test.yeokjeon.com → 포트 8080` 이므로, **`is in [on.yeokjeon.com, on-test.yeokjeon.com]`** 로 바꾸거나 규칙을 추가
3. **카카오 Developers** — JavaScript 키의 **허용 도메인에 새 주소 2개 등록**. ⚠ 빠뜨리면 다른 기능은 멀쩡한데 **지도만 조용히 안 뜬다**
4. **다우오피스** — 콜백 URL 을 등록해 둔 경우 새 주소로 변경

**순서 주의**: 새 도메인이 실제로 뜨는 것을 확인한 **뒤에** 구 `test` 레코드를 지울 것. 먼저 지우면 그동안 접속이 끊긴다.

### 35-L-보정. Cloudflare 라우팅 실제 구조 정정
Zero Trust 터널(`yon-field-server`) 의 Published application routes 를 사용자가 공유해 확인했다. **`one` / `one-test` 는 오타가 아니라 완전히 다른 애플리케이션**이다(포트 4000/3000, 4001/3010 대역 — 우리 ERP 와 무관, 같은 `yeokjeon.com` 존을 공유할 뿐). 앞서 오타로 추정해 삭제를 권했던 것은 잘못된 판단이었다.

실제 라우팅 표에서 확인된 문제:
- `on-test.yeokjeon.com` 이 **`one-test` 앱의 포트(4001/3010)를 그대로 물려받은 잘못된 규칙**으로 등록되어 있었다 — 우리 백엔드(3001)도 Caddy(8080)도 아니다
- `on.yeokjeon.com` 은 라우트 자체가 없었다(404)

우리 배포 구조(Caddy 하나가 8080에서 정적 파일 + `/api/*` 프록시를 전부 처리)는 `test.yeokjeon.com` 의 기존 규칙(`* → localhost:8080` 단일 규칙)과 동일해야 하므로, 사용자에게 다음을 안내했다:
- `on-test.yeokjeon.com` 의 기존 두 규칙(`api/*→4001`, `*→3010`) 삭제 후 `* → localhost:8080` 단일 규칙으로 교체
- `on.yeokjeon.com` 에 `* → localhost:8080` 규칙 신규 추가
- `one*` 계열은 손대지 말 것

### 35-L. 완료 확인 (2026-08-03 09:5x)
로그인 장애 수정본 JAR 배포와 관리자 계정 SQL 이 **모두 적용된 것을 실제 호출로 확인**했다.

- `GET /api/health` → 200, `GET /api/users`(토큰 없음) → 401 → 수정본 JAR 구동 중
- `POST /api/auth/login` (admin) → **200, 토큰 발급, adminYn=Y, 메뉴권한 19건** → 로그인 정상 복구
- 발급 토큰으로 조회: **가맹점 1,040건 / 물건 112건 / 예비창업자 5건 / 사원 69건** → 어제 "사라진 것처럼 보였던" 데이터가 모두 정상 조회됨(사원 69건은 목록에서 가맹점주 계정이 빠진 수치)

## 2026-08-03 — 사원관리 CSV 일괄 업로드: 확인 화면이 변경 내용을 못 보여주던 문제

### 37. 신고 내용
"업로드내용 확인 부분에서, 그냥 사원관리의 순서대로 나오는데? 업로드되서 적용되는 내용을 반영해줘야지."

원인: `_UploadPreviewDialog`(`mst001_view.dart`)는 CSV 값을 그대로 보여주고 있었다(코드 자체는 맞음). 하지만 실제 사용 흐름이 "전체 목록 다운로드 → 몇 명 정보만 수정 → 전체를 그대로 재업로드"이다 보니, 미리보기 표가 **대부분 사원관리 목록과 똑같은 값·순서**로 나와서 업로드가 실제로 뭘 바꾸는지 전혀 구분되지 않았다. 이전에 사용자가 명시적으로 요청했던 "diff 방식 말고 그냥 최종 데이터를 보여주는 방식"(2026-08-02) 자체는 유지하되, 실제로 달라지는 값만 눈에 띄게 표시해야 한다는 요청이었다.

### 38. 수정 내용
| `mst001_csv.dart` (신규 `UserCsvRowDiff`, `diffUserCsvRow`) | CSV 값과 기존 사원 값을 이름·부서·직급·휴대전화·이메일·태그사용여부 6개 필드별로 비교하는 순수 함수 | 판정 로직을 뷰 파일이 아니라 이미 테스트가 있는 모듈로 옮겨 실제 단위테스트로 검증 가능하게 함 |
| `mst001_view.dart` `_UploadPreviewDialog` | ① 상단에 "실제로 값이 바뀌는 항목 N건 (파란 글씨) · 기존과 동일 M건" 요약 문구 추가 ② 표에서 **실제로 달라지는 셀만 파란 굵은 글씨로 강조**(`AppTheme.statusRenewal`), 나머지는 그대로 | 최종 데이터 표시 방식은 그대로 두고, 무엇이 바뀌는지만 한눈에 보이게 함 — 재설계가 아니라 강조 추가 |

### 39. 검증
`test/mst001_csv_test.dart` 에 4종 신규 단위테스트 추가(기존 5종 + 신규 4종 = **9종 전부 통과**):
- CSV를 그대로 재업로드(무변경) → 변경 없음으로 판정
- 부서만 바뀜 → 부서만 변경 표시, 나머지는 변경 아님
- 태그사용여부(Y/N)만 바뀜 → 감지됨
- 여러 필드 동시 변경 → 전부 개별 감지됨

`flutter analyze` 는 기존에 있던 무관한 info 2건 외 신규 에러 없음. **주의**: 이 화면은 Flutter 캔버스 기반 파일 업로드 다이얼로그라 브라우저 자동화로 실제 클릭·파일선택까지는 이번 세션에서 확인하지 못했다 — 판정 로직(핵심)만 실제 단위테스트로 검증했고, 화면 렌더링 자체는 `flutter analyze` 통과로만 확인했다.

## 2026-08-03 — 사원관리 CSV: 로그인ID 일괄 변경 시 "못 찾음" + 미리보기 창 크기

### 40. 신고 내용
"로그인ID가 대부분 이름으로 되어 있어서 이메일 주소로 다 붙여넣으려고 일괄업로드를 했는데 (못 찾음)이라고 나온다." + "업로드 내용 확인 UI 창이 너무 작아서 오른쪽 열이 안 보인다."

### 41. 원인 — 로그인ID를 매칭 키로도 쓰고 있었다 (구조적 결함)
업로드는 "CSV의 로그인ID 값으로 기존 사원을 찾아" 그 사원을 갱신하는 방식이었다. 그런데 **로그인ID 자체를 바꾸는 게 목적**이면, CSV에 적은 새 값(이메일)이 기존 사원 누구의 현재 로그인ID와도 일치할 수 없다 — 매칭이 구조적으로 항상 실패한다. "값을 바꾸려고 적었는데 그 값으로 사람을 찾으려 한다"는 자기모순이었다. 오탈자나 매칭 로직 버그가 아니라 애초에 이 기능으로는 로그인ID 자체를 바꿀 방법이 없었다.

### 42. 수정 — 사번(userIdx)을 매칭 키로 분리
| `mst001_csv.dart` | CSV 첫 컬럼에 **`사번(수정 금지)`** 추가(`buildUsersCsv`가 `userIdx` 포함). `parseUsersCsv` 는 헤더 첫 칸이 `사번(수정 금지)`인지 보고 신·구 형식(8열/7열)을 모두 지원한다 — 이미 내려받아 둔 예전 파일도 계속 동작한다 | 사번은 절대 안 바뀌는 값이라, 이제 로그인ID를 마음껏 바꿔도 매칭이 유지된다 |
| `mst001_view.dart` `_uploadCsv` | 매칭을 **사번 우선, 없으면 로그인ID로 폴백**하도록 변경 | |
| 〃 `_UploadPreviewDialog` | 로그인ID 칸을 바뀔 때 **"기존ID → 새ID"** 로 보여줌(파란 글씨) | 사번이라는 숫자보다 "무엇이 무엇으로 바뀌는지"가 사람에게 더 명확함 |
| 신규 `findDuplicateTargetUserIds` | 같은 로그인ID를 목표로 하는 행이 CSV 안에 둘 이상이면 저장 전에 경고 문구 표시(막지는 않음) | 로그인ID는 DB에서 유일해야 하므로, 이름→이메일 전환 중 같은 이메일을 두 사람에게 잘못 적는 실수를 미리 잡기 위함 |

### 43. 수정 — 미리보기 창 크기·스크롤
고정 720×420 크기였는데, 컬럼이 늘고(사번 추가) 화면 크기가 작으면 오른쪽이 안 보이는데 스크롤 가능하다는 표시조차 없었다.
- `MediaQuery` 기준 반응형 크기로 변경(가로 화면의 92%, 최대 1180 / 세로 화면의 60%, 최대 560)
- 가로·세로 각각 **항상 보이는 스크롤바**(`Scrollbar(thumbVisibility: true)`) 추가 — 이전에는 스크롤이 가능한지조차 알 수 없었다

### 44. 검증
`test/mst001_csv_test.dart` 신규 8종 추가(기존 9종 + 신규 8종 = **17종 전부 통과**):
- 사번이 CSV 왕복에서 보존됨 / 사번 컬럼 없는 예전 파일도 파싱됨(하위호환)
- 로그인ID 자체가 바뀌면 감지됨
- **로그인ID가 완전히 다른 값(이메일)으로 바뀌어도 사번으로 매칭됨** — 이번 신고의 핵심 재현 테스트
- 사번 없는 예전 파일은 여전히 로그인ID로만 매칭됨(회귀 없음)
- 로그인ID 중복 지정 감지 3종

`flutter analyze` 기존 무관 info 2건 외 신규 에러 없음.

**주의**: 이미 내려받아 둔 CSV 파일(사번 컬럼 없는 예전 형식)로는 **로그인ID 자체를 바꾸는 업로드가 안 된다**(기존 동작 그대로 — 다른 필드 수정은 여전히 됨). 로그인ID를 이메일로 바꾸려면 **사원관리 화면에서 CSV를 다시 다운로드**해서(사번 컬럼 포함) 그 파일에 이메일을 적어야 한다.

## 2026-08-07 — 초기 비밀번호·비밀번호 초기화·사번 분리

### 45. 요청 내용
1. 초기 비밀번호가 뭘로 설정돼 있는지 확인하고, 없으면 `durwjs1982` 로 설정. 로그인ID가 부여되면 로그인 가능한 계정도 같이 생겨야 함
2. 업로드 매칭 기준값 표시를 "사번" 대신 "토큰번호"로 바꾸고, 회사가 실제 관리하는 "사번"은 별도 UI 항목으로 선택적 관리
3. 사원관리 "일괄 다운로드" 왼쪽에 "비밀번호 초기화" 버튼 — 누르면 초기 비밀번호로 변경, 이후 최초 로그인 시 비밀번호 변경 가능하도록

### 46. 확인 결과 — 초기 비밀번호는 지금까지 없었다
사원 등록 화면(`mst001_view_register.dart`)은 비밀번호를 **필수 입력**으로 받았고, 자동 생성되는 기본값은 없었다. 즉 관리자가 매번 직접 비밀번호를 정해 입력해야 했다.

### 47. 수정 — 초기 비밀번호 도입
| `application.yml` | `auth.default-password: ${DEFAULT_USER_PASSWORD:durwjs1982}` 추가 | `AUTH_TOKEN_SECRET` 과 같은 방식으로 `config\backend.env` 에서 값을 바꿀 수 있게 함 |
| `AuthService.java` | `defaultPassword()`, `resetPasswordToDefault(userId)`, `markPasswordResetRequired(userId)` 추가 | 비밀번호 재설정과 "다음 로그인 시 변경 강제" 플래그를 분리해서, 플래그 쪽이 실패해도(컬럼 미적용) 비밀번호 자체는 항상 반영되게 함(기존 `changePassword`/`clearPasswordResetFlag` 와 같은 방어 패턴) |
| `MstService.save(create)` | 등록 시 비밀번호를 비워두면 초기 비밀번호로 생성 + 강제변경 플래그 설정 | **로그인ID를 부여하면서 비밀번호를 안 정해도 실제로 로그인 가능한 계정이 만들어진다**(요청 1번의 핵심) |
| `UserMstCreateRequestDto.userPassword` | `@NotBlank` 제거 | 비워서 보내는 것을 허용 |
| `mst001_view_register.dart` | 비밀번호/비밀번호 확인을 선택 입력으로 변경, 힌트를 "비워두면 초기 비밀번호로 자동 생성됩니다"로 표시 | |

### 48. 수정 — "비밀번호 초기화" 버튼
`MstController` 에 `POST /users/{userIdx}/reset-password` 신설(사원관리 권한 요구) — 초기 비밀번호로 되돌리고, 강제변경 플래그를 세우고, 기존 로그인 토큰을 전부 끊는다.

프론트: 사원관리 "일괄 다운로드" **왼쪽**에 "비밀번호 초기화" 버튼 추가. 체크박스 다중선택 UI가 아직 없어, **화면 검색·필터로 좁혀진 목록을 대상**으로 삼는다(특정 한 명만 바꾸려면 검색으로 그 사람만 남기고 누르면 됨). 실행 전 대상자 명단·인원수를 보여주는 확인 다이얼로그를 거친다.

**주의**: "다음 로그인 시 변경 강제"가 실제로 동작하려면 `pwd_reset_yn` 컬럼이 있어야 한다(51번 참고). 컬럼이 없어도 비밀번호 초기화 자체는 정상 동작한다 — 다만 강제 변경 화면은 안 뜬다.

### 49. 수정 — 매칭 기준값 "사번" → "토큰번호", 별도 "사번" 필드 신설
지난 턴에 CSV 매칭 키로 도입한 내부 식별자(`userIdx`)를 "사번"이라 이름 붙였는데, 회사가 실제로 쓰는 사번(HR 사원번호) 개념과 섞이는 문제가 있었다. 분리했다:

| `mst001_csv.dart` | CSV 헤더 `사번(수정 금지)` → **`토큰번호(수정 금지)`** | 매칭 값 자체(`userIdx`)는 그대로, 이름만 "내부 시스템 값"임이 분명하도록 변경 |
| 신규 `EmpNoDto`, `EmpNoMapper`(+xml) | `GET/PUT /users/{userIdx}/emp-no` | **의도적으로 사원목록 핵심 조회(MstService/MstUser)와 완전히 분리** — 컬럼이 없어도 예외를 그 자리에서 잡아 사원 목록 전체에 영향이 없게 함(같은 방어 패턴을 반복 적용) |
| `mst001_view_detail.dart` 신규 `_EmpNoField` | 사원 상세 화면에 "사번" 입력칸 추가, 자체 조회·저장(메인 저장 흐름과 무관, 선택 입력) | 요청한 "따로 UI 항목에 추가하고 선택적으로 관리" 그대로 |

### 50. 검증
- 백엔드 신규 JUnit 테스트(`MstServiceDefaultPasswordTest`, `@SpringBootTest` + H2) **4종 전부 통과**:
  - 비밀번호를 비워서 등록 → 초기 비밀번호로 생성됨(실제 해시 비교로 확인)
  - 비밀번호를 직접 입력 → 그 값이 그대로 쓰임(초기 비밀번호가 아님도 함께 확인)
  - 관리자가 초기화 → 초기 비밀번호로 바뀜
  - **사번 컬럼이 없는 DB 에서 조회가 실제로 예외를 던지는 것까지 확인** — 방어 코드가 진짜로 뭔가를 막고 있다는 뜻
  - H2 테스트 DB 는 JPA 엔티티로만 스키마를 만들고 `pwd_reset_yn`/`emp_no` 는 의도적으로 엔티티에 없다 — 즉 이 테스트 환경 자체가 "컬럼 미적용 운영 DB" 상황이며, 실제로 그 컬럼 없음 예외가 로그에 찍히면서도 테스트는 통과했다(방어 코드가 실제로 동작 확인)
- `flutter analyze`: 기존 무관 info 2건 외 신규 에러 없음
- `flutter test test/mst001_csv_test.dart`: 기존 17종 그대로 통과(헤더 문구만 바뀌었을 뿐 값은 동일)

### 51. 사용자 조치 필요 — DB 마이그레이션(안전, 추가만 함)
`deploy/db/migrations/20260807_user_pwd_reset_yn_and_emp_no.sql` 신규 작성. **7/31 에 만들어 두고 실행하지 않기로 한 마이그레이션과는 다른 파일이다** — 그쪽은 전 직원 비밀번호를 통째로 재설정하는 되돌릴 수 없는 작업이고, 이번 것은 컬럼 2개를 추가만 한다(`ADD COLUMN IF NOT EXISTS`, 기존 비밀번호·데이터 무변경). 실행 안 해도 지금 만든 기능들은 전부 동작한다 — 다만:
- `pwd_reset_yn` 없으면: 비밀번호 초기화는 되지만 "다음 로그인 시 변경 강제"는 안 걸림
- `emp_no` 없으면: 사원 상세의 "사번" 저장 시 안내 문구만 뜨고 다른 기능엔 영향 없음

```
psql -h localhost -p 5433 -U postgres -d yj_db_test -f C:\y-on\yon-field-server\db\20260807_user_pwd_reset_yn_and_emp_no.sql
```
(파일을 서버의 `db` 폴더로 옮겨 실행할 것 — 인코딩 문제 있으면 `$env:PGCLIENTENCODING="UTF8"` 먼저 설정)

### 36. 사용자 조치가 필요한 항목 (내가 할 수 없는 것)
1. **도메인 전환** — 35-K 의 Cloudflare DNS·Origin Rule·카카오 도메인 등록. 새 주소가 뜨는 것을 확인한 뒤 구 `test` 레코드 삭제
2. **도메인용 JAR 교체** — 새 도메인의 CORS·WebSocket 허용이 담긴 JAR:
   ```
   powershell -ExecutionPolicy Bypass -File C:\y-on\yon-field-server\scripts\update-app.ps1 -ReleasePath C:\y-on\release\yon-field-server-20260803-0950
   ```
   웹은 재배포 불필요(35-J 실측). **DNS 전환 전에 미리 넣어두면** 새 주소로 바꾸는 순간 CORS 오류 없이 바로 붙는다
3. **GitHub 저장소를 비공개로** — 이력에 DB 비밀번호·카카오 키가 남아 있다
4. **유출된 자격증명 교체** — DB 비밀번호, 카카오 REST API 키, 그리고 **관리자 비밀번호도 DB 비밀번호와 다른 값으로**(35-H 참고)
5. `config\backend.env` 의 `AUTH_TOKEN_SECRET` 유지 — 미설정이면 재시작마다 전원 재로그인
6. (선택) `pwd_reset_yn` + `emp_no` 컬럼을 쓰려면 — 51번의 `20260807_user_pwd_reset_yn_and_emp_no.sql` 실행. 지금은 컬럼이 없어도 정상 동작하므로 **급하지 않다**

## 2026-08-13 — 배포 누락 사고: "비밀번호 초기화" 성공 응답이 거짓이었다

### 52. 무슨 일이 있었나
사용자 요청으로 전 직원(68명, admin 제외) 비밀번호를 초기값으로 일괄 초기화하는 작업을 라이브 API로 실행했다. 68명 전부 `200 성공` 응답을 받아 "성공"으로 보고했다.

**보고 직후 표본 검증(4명)에서 새 비밀번호로 로그인이 전부 401로 실패**하는 것을 발견했다. **"68명 성공"은 거짓 양성이었다.**

### 53. 원인 — 오늘 고친 버그의 수정본을 배포하지 않았다
이번 세션 앞부분에서 발견·수정한 "비밀번호 초기화가 트랜잭션째로 롤백되는" 치명적 버그(35-A 참고 계열, `AuthService.resetPasswordToDefault` 의 `@Transactional`)를 **소스 코드에서는 고치고 테스트까지 통과시켰지만, 실제 JAR을 빌드해 서버에 배포하는 단계를 빠뜨렸다.**

라이브 서버는 여전히 8/7 배포분(`yon-field-server-20260807-1630`)을 돌리고 있었다 — 즉 고쳐지지 않은 예전 버그가 그대로 실행 중이었다:
1. 비밀번호 UPDATE 실행(성공)
2. `pwd_reset_yn` UPDATE 시도 — 컬럼 유무 확인 안 됨(진단용으로 쓴 `emp-no` 엔드포인트는 try/catch 로 항상 200 을 반환하도록 설계돼 있어, 마이그레이션 적용 여부를 구분하지 못하는 부정확한 지표였다)
3. 컬럼이 없으면 PostgreSQL 이 트랜잭션 전체를 abort → 1번의 비밀번호 변경도 함께 롤백
4. 자바 예외는 삼켜져서 API는 200 성공을 반환

**다행인 점**: 트랜잭션 롤백은 전부 되돌리는 것이라, 68명의 기존 비밀번호는 그대로 남아 있을 것으로 판단된다(직접 확인은 못 했지만 트랜잭션 원자성상 확실). 데이터 손상이 아니라 "아무 일도 안 일어난 것"이었다.

### 54. 조치
- 오늘 세션에 고친 것 전부(트랜잭션 분리, check-user-id 파라미터명 변경, 프로필 응답 토큰 유지, 43개 엔드포인트 권한 가드)를 포함해 **JAR·웹 번들을 새로 빌드**
- 회귀 테스트(`PasswordResetFlagTransactionTest`, `MstServiceDefaultPasswordTest`) 재실행 — 11/11 통과
- 웹은 라이브 배포·실측 확인 완료(6,166,043 bytes)
- **백엔드 JAR은 아직 서버에 배포 안 됨** — 아래 사용자 조치 필요 참고
- 전 직원 비밀번호 일괄 초기화는 **JAR 교체 후 재실행 필요**

### 55. 배운 것
- 소스 수정 + 테스트 통과 + git 커밋은 "고쳤다"가 아니다. **빌드해서 실제로 돌아가는 서버에 올려야 고친 것이다.** 이번 세션 초반 로그인 장애 때 얻은 교훈("배포 확인은 HTTP 200 이 아니라 본문으로")과 같은 종류의 실수를 배포 자체를 빠뜨리는 방식으로 반복했다.
- 라이브에서 실제 동작(로그인 성공 여부)까지 검증하지 않고 API 의 `200 성공` 만 보고 보고했다 — 응답 코드가 항상 진실을 말하지 않는다는 걸 이미 알고 있었는데도 이번엔 그 확인을 건너뛰었다.
- 진단용으로 만든 방어적 엔드포인트(`emp-no`, try/catch 로 항상 200)를 "마이그레이션 적용 여부 확인"이라는 다른 목적에 잘못 재사용했다 — 방어 코드는 실패를 숨기도록 설계된 것이라 진단에는 못 쓴다.

### 56. 최종 완료 — 배포 확인 후 재실행
사용자가 서버에서 `update-app.ps1` 실행(JAR 교체) + `20260807_user_pwd_reset_yn_and_emp_no.sql` 실행(컬럼 추가) 완료.

**배포 검증**(추측 아님, 실측):
- `candidateUserId` 방식 정상 동작, 예전 `userId` 방식은 403 → 새 JAR 확인
- 표본 재초기화 → 즉시 로그인 성공 + `mustChangePassword=true` → 트랜잭션 수정·강제변경 플래그 모두 정상 동작 확인

**전 직원 비밀번호 초기화 완료**: 68명(관리자 `admin` 제외) → 초기 비밀번호 `durwjs1982`, 다음 로그인 시 변경 강제.
- API 응답: 68/68 성공
- **표본 5명(처음·중간·끝 분산) 실제 로그인으로 재검증**: 5/5 통과 — 전부 로그인 성공 + `mustChangePassword=true` 확인
- `admin` 계정은 이미 같은 비밀번호라 제외(재실행 시 진행 중이던 세션 토큰이 끊기는 부작용 회피)

## 2026-08-21 — 권한 가드 배포 후 전 직원 쓰기 불가 (내가 낸 장애)

### 57. 무슨 일이 있었나
8/13 배포한 JAR 에는 43개 쓰기 엔드포인트를 막는 `MenuAccessGuard` 가 들어 있었다. 그런데 **짝이 되는 권한 데이터를 채우지 않았다.** `user_menu_auth` 는 69명 중 66명이 전 컬럼 'N' 인 상태 그대로였고, `MenuAccessGuard.ensure` 는 권한이 없으면 거부(fail-closed)한다.

→ **일반 직원 전원이 가맹점·활동·개발 등 모든 등록·수정·삭제에서 403.** 조회만 되는 반쪽 상태로 배포되어 있었다.

라이브 실측으로 확인: 일반 직원(`dwkang@yeokjeonfnc.com`) 토큰으로 `PUT /api/stores/1` → `403 이 작업을 수행할 권한이 없습니다.`

**원인**: 코드(가드)와 데이터(권한 부여)가 한 쌍인데 코드만 배포했다. 배포 전 "일반 직원 계정으로 실제 업무가 되는지" 확인하지 않았다 — 관리자 계정으로만 검증해서 `isSuperAdmin` 우회에 가려 보이지 않았다.

### 58. 조치 — 관리자 API 로 즉시 복구
`20260812_user_menu_auth_baseline_grant.sql` 을 서버에서 실행하는 대신, 같은 내용을 **관리자 권한으로 REST API(`PUT /users/{userIdx}/menu-permissions`)를 호출해 적용**했다(psql 접근이 필요 없어 즉시 복구 가능).

부여 기준(SQL 과 동일):
- 조회: 전 메뉴 허용 → 사이드바 현행 유지, 체감 변화 없음
- 업무 메뉴 14개(dsh001·bbs001·str001·dev001~003·act001~004·eap001·mst002·mst004·mst007) 쓰기: 전원 허용
- 관리 메뉴 4개(mst001·mst003·mst005·mst006) 쓰기: 관리자(userIdx 5 `admin`, 59 `mhkim@yeokjeonfnc.com`)만

**결과 70/70 성공**, 라이브 재검증:
- 일반 직원 → 가맹점 수정 **200 정상 복구**, 쓰기가능 메뉴 14개 확인
- 일반 직원 → 사원 삭제 **403 유지**(관리 기능은 계속 차단) — 의도대로

### 59. 배운 것
- **가드(코드)와 권한(데이터)은 한 쌍이다.** 둘 중 하나만 배포하면 장애다. 이번엔 "SQL 은 사용자가 나중에 실행" 으로 미뤄뒀는데, 그 사이가 곧 장애 구간이었다.
- **권한 변경은 관리자 계정으로 검증하면 안 된다.** 슈퍼관리자는 모든 검사를 우회하므로 아무 문제가 없어 보인다. 반드시 **일반 권한 계정으로** 실제 업무 흐름을 확인해야 한다.
- 참고: 이후 `20260812_user_menu_auth_baseline_grant.sql` 은 이미 API 로 반영됐으므로 **실행할 필요 없다**(실행해도 같은 값이라 무해).

## 2026-08-21 — 깃허브(다른 개발자 작업)에서 전자결재만 골라서 반영

### 60. 어디를 / 무엇을
공유 브랜치(`origin/codex/field-test-security-hardening-20260813`)에 다른 개발자가 올린 커밋 11개 중 **전자결재(EAP) 부분만** 로컬에 반영했다. 나머지(사원관리 퇴사자 처리, 가맹점 검색조건, 영업지역 지도, 예비창업자 이메일 중복확인, 게시판 헤더)는 **일부러 가져오지 않았다.**

**통째로 가져온 것(EAP 전용)**
- `app_flutter/lib/pages/eap/` — 8개 → 31개 파일(기안 작성, 문서함, 서식 편집기, HTML 에디터, 결재선 패널 등)
- `app_flutter/web/eap_*.js|css|html` — 서식 편집기·미리보기·문서 표 렌더러 13개 (신규)
- `backend/.../eap/` — 4개 → 21개 파일(`EapDocumentService`, `EapFormConfigService`, `EapFolderMatcher`, 매퍼·DTO)
- `backend/src/main/resources/mapper/eap/` — 매퍼 XML 3개 (신규)

**EAP 화면이 실제로 import 해서 같이 가져온 공용 파일(모두 선택적 파라미터 추가 = 기존 화면 동작 불변)**
- `act002_approval_table.dart` (신규 결재정보 표 위젯 — 현재 EAP 에서만 사용)
- `act002_dialog_approval_line.dart` — `blockedKeys`/`blockedMessage` 선택 파라미터 추가(기본값이 빈 집합이라 활동관리 동작은 그대로)
- `detail_screen_scaffold.dart` — `DetailScreenWithTabs(toolbar:)` 선택 파라미터 추가
- `api_client.dart` — `ApiReachability`(백엔드 재기동 감지) 추가
- `MenuAccessGuard.java` — `ensureAny()` 추가 + 메뉴코드 대소문자 무시 비교
- `menu_codes.dart` / `MenuCodes.java` / `menu_route_access.dart` — `mst007`(서식관리) 추가

**사이드바 구조 변경(전자결재)**
- 이전: 결재대기·수신·참조/열람대기·결재예정 / 기안·임시저장·결재·참조열람·수신·발송·공문 문서함 + 환경설정 (13개)
- 이후: **기안하기 · 받은결재 · 올린결재 · 수신참조결재 · 전체문서** (5개)
- 마스터 관리 밑에 **서식관리** 신설
- 옛 주소(`/eap/pending` 등)는 라우터에서 새 화면으로 자동 연결 — 즐겨찾기·알림 링크가 깨지지 않는다.

### 61. 어떻게 — 안 가져온 것과 그 이유
| 안 가져온 것 | 이유 |
|---|---|
| `MenuPermissionMapper.xml` 의 `AND m.menu_type = 'L'` | 실 DB 의 `menu_mst.menu_type` 값이 확인 안 된 상태에서 켜면 **전 직원 메뉴 권한이 통째로 사라질 수 있다**(8/13 장애와 같은 종류). EAP 동작에 필요 없다. |
| `application.yml` 의 다우 인증키 하드코딩 | 원격에는 `client-id`/`client-secret` 실값이 기본값으로 박혀 있다. **공개 저장소에 인증키가 남는다.** 확인해 보니 이번 EAP 구현은 다우 API 를 아예 호출하지 않는 ERP 자체 결재라 인증키가 필요 없어서, 기존 환경변수 전용 설정을 유지했다. |
| `application.yml` 의 `callback-url` | 원격 기본값이 폐기된 옛 도메인(`test.yeokjeon.com`)으로 되돌아가 있었다. |
| `YeokjeonErpApplication` 의 `TimeZone.setDefault(Asia/Seoul)` + `connection-init-sql` | 전 메뉴의 날짜 표시·저장에 영향을 주는 전역 변경이라 EAP 만 적용하는 이번 범위에 넣지 않았다. 별도로 검증하고 반영할 것. |
| `AuthTokenService` 의 로컬 서명키 파일 저장 | 개발 편의 기능이고 보안 영향이 있어 별도 판단이 필요하다. EAP 와 무관. |
| `AuthTokenFilter`/`WebConfig` 의 CORS 필터 전환 | 401/403 응답에 CORS 헤더를 붙이는 별개 버그 수정. EAP 와 무관해서 보류. |
| mst001·mst006·str001·dev001·dev003·bbs001·act002 화면 변경 | 요청 범위(전자결재)가 아니다. |

**내 8/21 수정 중 되살린 것**(원격 파일로 덮이면서 사라질 뻔한 것)
- `main_frame_layout.dart` — 활동 계획 메뉴를 `can(kMenuAct004)` 로만 노출(act002 까지 열면 라우터 가드에 튕긴다)
- `app_router.dart` — 화면 이동 시 전체 캐시 무효화 대신 `refreshRouteScreenData` 로 들어간 화면만 갱신
- `app_data_refresh.dart` — 위 구조를 유지하면서 EAP 프로바이더 무효화 추가

**내 8/21 EAP 수정 중 없어진 것**(문제 없음)
- 다우 연결테스트가 진짜 결재문서를 만들던 버그를 HEAD 요청으로 고쳤었는데, 새 구현은 **연결테스트 기능(환경설정 화면)과 `EapConnectionService` 자체를 삭제**했다. 원인이 사라졌으므로 수정도 함께 제거했다.

### 62. 왜 — 빠져 있던 DB 마이그레이션을 새로 작성
원격 브랜치에는 **EAP 테이블을 만드는 SQL 이 한 건도 없었다.** 매퍼가 쓰는 `erp_approval_mappings` / `eap_doc_line` / `eap_form_config` 가 없으면 전자결재 화면이 전부 500 으로 떨어진다. 매퍼 XML 이 실제 쓰는 컬럼만 그대로 옮겨 두 파일을 새로 만들었다.

- `deploy/db/migrations/20260821_eap_approval_tables.sql` — 위 3개 테이블 + 인덱스
- `deploy/db/migrations/20260821_menu_mst007_eap_forms.sql` — `menu_mst` 에 `mst007`(서식관리) 등록 + 전 사용자 권한 행(조회 Y / 쓰기 N) 생성

둘 다 `IF NOT EXISTS` / `ON CONFLICT` / "없는 행만 INSERT" 라서 이미 만들어진 서버에서 다시 돌려도 기존 데이터를 지우거나 좁히지 않는다.

> **8/13 장애에서 배운 것을 그대로 적용했다 — 가드(코드)와 권한(데이터)은 한 쌍이다.**
> `EapController` 는 서식관리 API 를 `mst007` 권한으로 막는다. 코드만 올리고 `menu_mst`·`user_menu_auth` 를 안 채우면 관리자 외 전원이 403 이 된다.

### 63. 검증
- 백엔드 컴파일 통과, **테스트 12개 전부 통과**(`PasswordResetFlagTransactionTest` 5, `MstServiceDefaultPasswordTest` 6, `contextLoads` 1)
- `flutter analyze` **에러 0** (남은 7건은 전부 info/warning 수준의 린트)
- 주의: 삭제한 `EapConnectionService` 의 `.class` 가 `target/` 에 남아 있으면 스프링이 그 빈을 만들려다 컨텍스트 기동에 실패한다. 빌드 전 `target/classes/.../eap/service/EapConnectionService*.class` 를 지울 것(또는 clean 빌드).

### 64. 서버 반영 시 순서 (아직 안 함)
1. `20260821_eap_approval_tables.sql` 실행
2. `20260821_menu_mst007_eap_forms.sql` 실행
3. JAR + web 빌드 → 릴리즈 폴더 → `update-app.ps1`
4. **일반 직원 계정**으로 전자결재 메뉴 진입·기안 작성까지 확인(관리자 계정으로만 보면 권한 문제가 안 보인다)
