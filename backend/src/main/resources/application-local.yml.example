# 로컬 개발 환경 설정 예시
# 실제 사용 시: application-local.yml 로 복사하여 사용

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yeokjeon_db
    username: postgres
    password: your_password_here  # 실제 비밀번호로 변경
